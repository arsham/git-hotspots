schema: flow-feature.v1
feature:
  id: "0014"
  slug: ergonomic-history-windows
  title: Large-repo history recipes
  lane: standard
  status: ready
  autonomy_default: ask
intent:
  problem: Large repositories can take noticeably longer to analyse across full
    reachable history. Users need clear guidance for deterministic bounded runs
    using the CLI options that already exist, without changing the default
    full-history product contract or adding ambiguous named history-window
    syntax prematurely.
  outcome: Add public documentation for large-repository recipes using existing
    --since REV, --scope, --include-prefix, and --progress options. Preserve all
    runtime behaviour, including default full-history analysis when --since is
    omitted. Defer named history-window syntax until deterministic semantics and
    validation cost are worth the product surface.
  acceptance:
    - README includes a concise large-repository recipe section.
    - Recipes use existing supported options only and do not introduce named
      history-window syntax as product behaviour.
    - Recipes include a project-focused bounded run using --scope project,
      --since HEAD~500, and --progress.
    - Recipes include a source-focused bounded run using --include-prefix src/
      and a bounded --since revision.
    - Docs clearly say omitting --since keeps full reachable local history.
    - Docs frame bounded runs as exploratory scopes, not more correct answers or
      hidden defaults.
    - Runtime CLI behaviour, scoring, filtering, progress, inspect, report
      schemas, and validation commands remain unchanged except for docs-only
      wording if needed.
    - README remains visitor-facing and free of Flow process exposition,
      commercial strategy, bug-prediction claims, objective quality-score
      claims, and developer-ranking claims.
    - If real-repo evidence docs change, committed content remains privacy-safe
      and contains no private path, repo name, remote URL, author identity,
      commit message, source snippet, or raw sibling report.
planning:
  state: ready
  summary: Feature 0014 documents large-repository recipes using existing
    deterministic Git revision syntax such as --since HEAD~500 together with
    --scope project, --include-prefix, and --progress. It intentionally defers
    named history-window syntax and preserves the default full-history runtime
    contract.
  brief_sync:
    status: updated
    reason: "Feature 0014 remains within B001: deterministic local-first file-level
      evidence, no hidden default filtering, no runtime LLM truth, no providers,
      no cache, no network or telemetry, and hotspots framed as investigation
      prompts."
    checked_at: 2026-05-22T20:45:00.000Z
  brief_alignment:
    status: aligned
    role: follow_up
    summary: "Follow-up documentation slice for B001: improves large-repo usage
      guidance without changing product truth or adding ambiguous syntax."
    checked_at: 2026-05-22T20:45:00.000Z
proposal:
  lifecycle_state: shaping
  outcome_contract_ref: null
  risk_assessment:
    assurance_level: routine
    reasons:
      - docs_only_feature
      - no_runtime_behaviour_change
      - validation_command_available
    risk_triggers:
      - public_docs_change
    audit_required: true
    judge_review_recommended: false
    materialised_at: 2026-05-22T20:45:00.000Z
  quality_plan: null
project_feature:
  lifecycle_state: ready
  outcome_contract_ref:
    contract_kind: feature_intent
    contract_ref: feature.intent
    materialised_at: 2026-05-22T20:45:00.000Z
  quality_plan:
    assurance_band: routine
    review_mode: control_plane_review
    reviewer_member_usage: optional
    reviewer_expectations:
      - verify docs against PRD and acceptance
      - confirm no unsupported runtime syntax is documented as implemented
    verification_contract:
      claim_classes: []
      evidence_policy:
        parity_claims: null
        synthetic_only_is_insufficient: false
      route_matrix:
        required: false
        routes: []
      resolution_matrix:
        required: false
        cases: []
      operator_examples:
        required: false
        cases: []
    derived_from_risk_assessment_ref: proposal.risk_assessment
    materialised_at: 2026-05-22T20:45:00.000Z
  authority_refinement:
    selected_sign_off_authority: orchestrator
    explicit_pause_required: false
    verification_independence_requirement: control_plane_allowed
    materialised_at: 2026-05-22T20:45:00.000Z
verification_independence_requirement: control_plane_allowed
readiness:
  requirements_ready: true
  acceptance_ready: true
  packet_plan_ready: true
  dependencies_ready: true
  implementation_ready: true
context:
  roadmap_item_id: null
  detached_reason: No active or candidate roadmap work existed; runtime evidence
    showed large repos benefit from explicit recent-history guidance before
    cache work.
  depends_on_feature_ids:
    - "0010"
    - "0012"
    - "0013"
  blocked_by: []
design:
  rough_shape:
    - Keep runtime unchanged; this is a documentation and usage-guidance slice.
    - Add README large-repo recipes using existing --since REV, --scope,
      --include-prefix, and --progress options.
    - Preserve named history-window shortcuts as a deferred design candidate,
      not implemented product syntax.
  packets:
    - id: P1
      depends_on: []
      status: planned
      title: Public large-repo recipes
      goal: Document practical deterministic command recipes for large repositories
        using existing CLI options only.
      owner_phase: runner
      execution_context:
        - "Anchors: README.md; optional docs/real-repo-validation.md only if a
          privacy-safe clarification is useful."
        - "In scope: large-repo recipes using --since HEAD~500, --since
          HEAD~1000, --scope project, --include-prefix src/, --progress, and
          existing report formats."
        - "Out of scope: named history-window syntax, changing defaults, date or
          duration parsing, cache, streaming, providers, CI, releases, network,
          telemetry, and performance claims."
        - "Protected: runtime code, scoring, report schemas, validation scripts,
          help text, and existing CLI behaviour unless a docs mismatch requires
          a docs-only correction."
        - "Dependencies: existing --since REV behaviour, project scope presets,
          include-prefix filters, and progress feedback."
        - "Validation/evidence: zig build validate, git diff --check, help
          smoke, README scans for unsupported syntax and prohibited claims."
        - "Review focus: docs are public, polished, deterministic, and do not
          imply bounded windows are hidden defaults or superior truth."
        - Escalate if useful docs require new runtime syntax, runtime behaviour
          changes, private repo details, or broader performance claims.
        - "Done boundary: README gives users concrete large-repo recipes and the
          repo validates cleanly with no unsupported product promises."
      verification_commands:
        - zig build validate
        - git diff --check
        - ./zig-out/bin/git-hotspots --help
  packet_contract:
    version: v1
    owner_model: phase_owned
    review_sequence:
      - spec_compliance
      - code_quality
    verification_required: false
    required_packet_fields:
      - id
      - title
      - goal
      - depends_on
      - status
      - owner_phase
      - execution_context
      - verification_commands
execution:
  active_run_id: null
  current_packet_id: null
  next_packet_ids: []
  blockers: []
  requires_confirmation: true
links:
  epic_todo_id: null
  brief_ids:
    - B001
timestamps:
  created_at: 2026-05-22T19:39:14.147Z
  updated_at: 2026-05-22T19:46:50.614Z
dispatch:
  status: prepared
  prepared_at: 2026-05-22T19:46:50.614Z
  assurance_level: routine
  control_model: split
  execution_topology: serial_feature
  model_plan:
    control_plane_model: Balanced
    runner_model: Fast
    review_mode: control_plane_review
    review_model: Balanced
    reviewer_member_usage: optional
    research_model: null
    scaffold_owner_model: null
    fill_worker_model: null
    escalation_triggers:
      - repeated validation failures
      - dispatch envelope no longer fits implementation reality
      - public interface, persistence, security, or concurrency change
  execution_fit:
    fast_execute_eligible: true
    reason: Scope is bounded and suitable for Fast execution.
  scope:
    mode: packets
    feature_id: "0014"
    packet_ids:
      - P1
  comparison_basis:
    roadmap_item_id: null
    linked_brief_ids:
      - B001
    feature:
      title: Large-repo history recipes
      problem: Large repositories can take noticeably longer to analyse across full
        reachable history. Users need clear guidance for deterministic bounded
        runs using the CLI options that already exist, without changing the
        default full-history product contract or adding ambiguous named
        history-window syntax prematurely.
      outcome: Add public documentation for large-repository recipes using existing
        --since REV, --scope, --include-prefix, and --progress options. Preserve
        all runtime behaviour, including default full-history analysis when
        --since is omitted. Defer named history-window syntax until
        deterministic semantics and validation cost are worth the product
        surface.
      acceptance:
        - README includes a concise large-repository recipe section.
        - Recipes use existing supported options only and do not introduce named
          history-window syntax as product behaviour.
        - Recipes include a project-focused bounded run using --scope project,
          --since HEAD~500, and --progress.
        - Recipes include a source-focused bounded run using --include-prefix
          src/ and a bounded --since revision.
        - Docs clearly say omitting --since keeps full reachable local history.
        - Docs frame bounded runs as exploratory scopes, not more correct
          answers or hidden defaults.
        - Runtime CLI behaviour, scoring, filtering, progress, inspect, report
          schemas, and validation commands remain unchanged except for docs-only
          wording if needed.
        - README remains visitor-facing and free of Flow process exposition,
          commercial strategy, bug-prediction claims, objective quality-score
          claims, and developer-ranking claims.
        - If real-repo evidence docs change, committed content remains
          privacy-safe and contains no private path, repo name, remote URL,
          author identity, commit message, source snippet, or raw sibling
          report.
    brief_alignment:
      status: aligned
      role: follow_up
      summary: "Follow-up documentation slice for B001: improves large-repo usage
        guidance without changing product truth or adding ambiguous syntax."
      checked_at: 2026-05-22T20:45:00.000Z
    briefs:
      - id: B001
        title: Local-first hotspot intelligence CLI
        summary: "Define the OSS capability for git-hotspots as a deterministic,
          local-first hotspot intelligence CLI for developers and coding agents:
          before refactoring, ask Git where change pressure and maintenance pain
          concentrate."
        desired_capability:
          - A command-line tool that analyses local Git history and produces
            reproducible hotspot evidence for files first, with symbol-level
            insight added later through provider adapters.
          - Reports should help humans and coding agents focus review,
            refactoring, testing, and onboarding effort without treating metrics
            as runtime truth or quality judgement.
          - Provider architecture should allow future evidence sources such as
            tree-sitter, LSP, ctags, dependency graphs, blame/history, and test
            or coverage data without making any single provider the core
            product.
        why:
          - "High change frequency is a useful attention signal: code that
            changes often may be fragile, strategically important, under-tested,
            or worth improving."
          - The reboot should avoid the old parser-heavy trap and prove
            usefulness with deterministic Git-history evidence before deep
            language support.
          - Local-first reports preserve trust and make the project suitable for
            private repositories, OSS maintainers, and agent-assisted workflows.
        in_scope:
          - OSS/publicity-oriented CLI capability and public project framing.
          - "File-level hotspot analysis as the first useful layer: churn,
            change frequency, recency, co-change, size, confidence, and
            explainable evidence."
          - Agent-ready export formats such as JSON and Markdown that users may
            pipe into their own tools or agents.
          - First-class provider concept for later symbol and dependency
            evidence, with tree-sitter as one provider rather than the
            foundation.
          - "Campaign language such as: Your git history knows where your
            technical debt lives; Stop refactoring by vibes; Before asking an AI
            to refactor, ask Git where to look; The riskiest code is not always
            the ugliest code, it is the code everyone keeps changing."
        out_of_scope:
          - Hosted dashboards, SaaS, and commercial or monetisation plans in
            repository artefacts.
          - Claims that the tool predicts bugs, measures code quality, ranks
            developers, or replaces established code-health platforms.
          - Mandatory LLM use at runtime; agents may consume exports, but the
            product truth remains deterministic.
          - Making tree-sitter, LSP, or any language provider part of the
            initial critical path.
          - Feature-level execution plans, PRDs, packet plans, todos, or runs
            during this brief-capture step.
        durable_decisions:
          - "Adopt the public thesis: deterministic, local-first hotspot
            intelligence CLI for developers and coding agents."
          - "Adopt the tagline: Before you refactor, ask Git where the pain is."
          - Start conceptually with file-level Git-history evidence; defer
            symbol-level precision until provider seams are validated.
          - Treat tree-sitter as a later provider, alongside possible LSP,
            ctags, dependency, blame/history, and test/coverage providers.
          - Keep the public repository free of monetisation plans and SaaS
            strategy; use it as OSS/publicity framing only.
          - Use agent-ready reports as an export surface, not as model-driven
            product truth.
    prd_path: .flow/features/0014_ergonomic-history-windows/prd.md
  requirements_contract:
    source_path: .flow/features/0014_ergonomic-history-windows/prd.md
    source_digest: a19ccd70067c6cce30581ffd586ffb0ef5edc3ca1e74666aeaee72cf04222ffd
    extraction: structured_markdown
    requirements:
      - id: REQ-001
        source_ref: prd.md#in-scope[2]
        text: Optional refresh to docs/real-repo-validation.md only if it improves the
          explanation of existing evidence without adding private details.
        category: requirement
        packet_refs:
          - P1
        verification: null
        required_for_close_out: true
      - id: REQ-002
        source_ref: prd.md#in-scope[3]
        text: Optional project memory note that named history windows are deferred, not
          rejected.
        category: requirement
        packet_refs:
          - P1
        verification: null
        required_for_close_out: true
      - id: REQ-003
        source_ref: prd.md#in-scope[4]
        text: Validation that docs remain public, non-commercial, non-judgemental, and
          consistent with current CLI behaviour.
        category: requirement
        packet_refs:
          - P1
        verification: null
        required_for_close_out: true
      - id: REQ-004
        source_ref: prd.md#out-of-scope[1]
        text: Named history-window syntax such as --since last-30d, --since 12m, or
          --since recent.
        category: non_goal
        packet_refs:
          - P1
        verification: null
        required_for_close_out: false
      - id: REQ-005
        source_ref: prd.md#out-of-scope[2]
        text: Changing the default history range.
        category: non_goal
        packet_refs:
          - P1
        verification: null
        required_for_close_out: false
      - id: REQ-006
        source_ref: prd.md#out-of-scope[3]
        text: Date, duration, or calendar parsing.
        category: non_goal
        packet_refs:
          - P1
        verification: null
        required_for_close_out: false
      - id: REQ-007
        source_ref: prd.md#out-of-scope[4]
        text: Repo-size auto-windowing.
        category: non_goal
        packet_refs:
          - P1
        verification: null
        required_for_close_out: false
      - id: REQ-008
        source_ref: prd.md#out-of-scope[5]
        text: Cache, streaming history ingestion, provider work, CI, packaging,
          releases, telemetry, network access, or performance claims.
        category: non_goal
        packet_refs:
          - P1
        verification: null
        required_for_close_out: false
      - id: REQ-009
        source_ref: prd.md#out-of-scope[6]
        text: Public third-party case studies or raw sibling-repo output.
        category: non_goal
        packet_refs:
          - P1
        verification: null
        required_for_close_out: false
      - id: REQ-010
        source_ref: prd.md#requirements[1]
        text: README includes a section such as Large repository recipes.
        category: requirement
        packet_refs:
          - P1
        verification: null
        required_for_close_out: true
      - id: REQ-011
        source_ref: prd.md#requirements[2]
        text: Recipes use existing supported options only.
        category: requirement
        packet_refs:
          - P1
        verification: null
        required_for_close_out: true
      - id: REQ-012
        source_ref: prd.md#requirements[3]
        text: "Recipes include at least:"
        category: requirement
        packet_refs:
          - P1
        verification: null
        required_for_close_out: true
      - id: REQ-013
        source_ref: prd.md#requirements[4]
        text: a project-focused recent exploratory run using --scope project, --since
          HEAD~500, and --progress;
        category: requirement
        packet_refs:
          - P1
        verification: null
        required_for_close_out: true
      - id: REQ-014
        source_ref: prd.md#requirements[5]
        text: a source-focused run using --include-prefix src/ and a bounded --since
          revision;
        category: requirement
        packet_refs:
          - P1
        verification: null
        required_for_close_out: true
      - id: REQ-015
        source_ref: prd.md#requirements[6]
        text: a full-history reminder explaining that omitting --since keeps full
          reachable local history.
        category: requirement
        packet_refs:
          - P1
        verification: null
        required_for_close_out: true
      - id: REQ-016
        source_ref: prd.md#requirements[7]
        text: Docs do not imply that bounded runs are more correct than full-history
          runs; they are exploratory scopes.
        category: requirement
        packet_refs:
          - P1
        verification: null
        required_for_close_out: true
      - id: REQ-017
        source_ref: prd.md#requirements[8]
        text: Docs do not claim speedups beyond reducing the selected history/output for
          an explicit bounded query.
        category: requirement
        packet_refs:
          - P1
        verification: null
        required_for_close_out: true
      - id: REQ-018
        source_ref: prd.md#requirements[9]
        text: Docs preserve local-first, no-telemetry, no-network, non-judgemental
          framing.
        category: requirement
        packet_refs:
          - P1
        verification: null
        required_for_close_out: true
      - id: REQ-019
        source_ref: prd.md#requirements[10]
        text: Existing CLI help and runtime behaviour remain unchanged unless validation
          exposes a documentation mismatch requiring a docs-only correction.
        category: requirement
        packet_refs:
          - P1
        verification: null
        required_for_close_out: true
      - id: REQ-020
        source_ref: prd.md#edge-cases-and-cautions[1]
        text: Do not recommend shell calendar expressions as product syntax.
        category: edge_case
        packet_refs:
          - P1
        verification: null
        required_for_close_out: true
      - id: REQ-021
        source_ref: prd.md#edge-cases-and-cautions[2]
        text: Do not use private local paths or raw sibling-repo output in committed
          docs.
        category: edge_case
        packet_refs:
          - P1
        verification: null
        required_for_close_out: true
      - id: REQ-022
        source_ref: prd.md#edge-cases-and-cautions[3]
        text: Do not introduce examples that require network access or remote fetching.
        category: edge_case
        packet_refs:
          - P1
        verification: null
        required_for_close_out: true
      - id: REQ-023
        source_ref: prd.md#edge-cases-and-cautions[4]
        text: Keep README visitor-facing; do not expose Flow process detail.
        category: edge_case
        packet_refs:
          - P1
        verification: null
        required_for_close_out: true
      - id: REQ-024
        source_ref: prd.md#verification[1]
        text: "Minimum validation before close-out:"
        category: verification
        packet_refs:
          - P1
        verification: "Minimum validation before close-out:"
        required_for_close_out: true
      - id: REQ-025
        source_ref: prd.md#verification[2]
        text: zig build validate
        category: verification
        packet_refs:
          - P1
        verification: zig build validate
        required_for_close_out: true
      - id: REQ-026
        source_ref: prd.md#verification[3]
        text: git diff --check
        category: verification
        packet_refs:
          - P1
        verification: git diff --check
        required_for_close_out: true
      - id: REQ-027
        source_ref: prd.md#verification[4]
        text: git-hotspots --help smoke, proving docs did not require unsupported flags.
        category: verification
        packet_refs:
          - P1
        verification: git-hotspots --help smoke, proving docs did not require
          unsupported flags.
        required_for_close_out: true
      - id: REQ-028
        source_ref: prd.md#verification[5]
        text: README scan for unsupported named history-window syntax as product syntax.
        category: verification
        packet_refs:
          - P1
        verification: README scan for unsupported named history-window syntax as product
          syntax.
        required_for_close_out: true
      - id: REQ-029
        source_ref: prd.md#verification[6]
        text: README scan for commercial/SaaS/pricing/sales strategy.
        category: verification
        packet_refs:
          - P1
        verification: README scan for commercial/SaaS/pricing/sales strategy.
        required_for_close_out: true
      - id: REQ-030
        source_ref: prd.md#verification[7]
        text: README scan for bug-prediction, objective quality-score, or
          developer-ranking claims.
        category: verification
        packet_refs:
          - P1
        verification: README scan for bug-prediction, objective quality-score, or
          developer-ranking claims.
        required_for_close_out: true
      - id: REQ-031
        source_ref: prd.md#verification[8]
        text: If docs/real-repo-validation.md changes, privacy scan proving no private
          path, repo name, remote URL, author identity, raw sibling output,
          commit message, or source snippet was added.
        category: verification
        packet_refs:
          - P1
        verification: If docs/real-repo-validation.md changes, privacy scan proving no
          private path, repo name, remote URL, author identity, raw sibling
          output, commit message, or source snippet was added.
        required_for_close_out: true
      - id: REQ-032
        source_ref: prd.md#verification[9]
        text: Close-out does not require sibling-repo smoke because this is a docs-only
          feature and does not change runtime behaviour. The existing canonical
          validation still includes this-repo smoke.
        category: verification
        packet_refs:
          - P1
        verification: Close-out does not require sibling-repo smoke because this is a
          docs-only feature and does not change runtime behaviour. The existing
          canonical validation still includes this-repo smoke.
        required_for_close_out: true
    supporting_context:
      - source_ref: prd.md#problem[1]
        text: Feature 0013 added opt-in progress feedback for long runs. Real-repo
          validation showed that large repositories can still take noticeably
          longer when analysing all reachable history. Users need guidance for
          choosing smaller evidence windows and source-focused scopes using the
          CLI surface that already exists.
      - source_ref: prd.md#problem[2]
        text: The README currently documents --since as an option, but it does not
          provide large-repo recipes or explain that existing Git revision
          syntax can be used to bound history deterministically.
      - source_ref: prd.md#outcome[1]
        text: "Add a concise public documentation section with large-repo recipes. The
          section must explain that:"
      - source_ref: prd.md#outcome[2]
        text: omitting --since keeps full-history behaviour;
      - source_ref: prd.md#outcome[3]
        text: --since REV means analyse REV..HEAD using Git revision syntax;
      - source_ref: prd.md#outcome[4]
        text: bounded examples such as HEAD~500 and HEAD~1000 are deterministic and
          useful for faster exploratory runs;
      - source_ref: prd.md#outcome[5]
        text: --scope project and --include-prefix can focus reports before heavier
          future features such as cache or providers exist;
      - source_ref: prd.md#outcome[6]
        text: --progress gives stderr-only feedback during long local analysis.
      - source_ref: prd.md#outcome[7]
        text: No runtime CLI behaviour changes in this feature.
      - source_ref: prd.md#in-scope[1]
        text: README large-repository recipe section.
    notes:
      - Extracted requirements from PRD requirement, acceptance, behaviour,
        constraint, edge-case, verification, or case sections.
      - Supporting context and structural headings are not close-out required;
        kept 10 refs outside default requirement rows.
  acceptance_contract:
    outcome: Add public documentation for large-repository recipes using existing
      --since REV, --scope, --include-prefix, and --progress options. Preserve
      all runtime behaviour, including default full-history analysis when
      --since is omitted. Defer named history-window syntax until deterministic
      semantics and validation cost are worth the product surface.
    acceptance:
      - README includes a concise large-repository recipe section.
      - Recipes use existing supported options only and do not introduce named
        history-window syntax as product behaviour.
      - Recipes include a project-focused bounded run using --scope project,
        --since HEAD~500, and --progress.
      - Recipes include a source-focused bounded run using --include-prefix src/
        and a bounded --since revision.
      - Docs clearly say omitting --since keeps full reachable local history.
      - Docs frame bounded runs as exploratory scopes, not more correct answers
        or hidden defaults.
      - Runtime CLI behaviour, scoring, filtering, progress, inspect, report
        schemas, and validation commands remain unchanged except for docs-only
        wording if needed.
      - README remains visitor-facing and free of Flow process exposition,
        commercial strategy, bug-prediction claims, objective quality-score
        claims, and developer-ranking claims.
      - If real-repo evidence docs change, committed content remains
        privacy-safe and contains no private path, repo name, remote URL, author
        identity, commit message, source snippet, or raw sibling report.
  validation_contract:
    ladders:
      - V2_atomic_commit
      - V3_final_packet
      - V4_close_out
    review_sequence:
      - spec_compliance
      - code_quality
    packet_verification_commands:
      - zig build validate
      - git diff --check
      - ./zig-out/bin/git-hotspots --help
    reviewer_must_repeat: true
    claim_classes: []
    evidence_policy:
      parity_claims: null
      synthetic_only_is_insufficient: false
    route_matrix:
      required: false
      routes: []
    resolution_matrix:
      required: false
      cases: []
    operator_examples:
      required: false
      cases: []
  ownership:
    reviewer_expectations:
      - verify docs against PRD and acceptance
      - confirm no unsupported runtime syntax is documented as implemented
  decision_budget:
    allowed_local_decisions:
      - safe internal refactor inside dispatched scope
      - local implementation detail changes that do not alter contracts
    deferred_reconcile_triggers:
      - minor non-contract deviation worth reviewer visibility
      - implementation trade-off that stays inside acceptance
    hard_planner_escalation_triggers:
      - scope or acceptance change
      - dependency or prerequisite change
      - protected-surface mutation outside dispatch envelope
    protected_surfaces: []
  fallback_path: deferred_reconcile_decision
  notes: null
