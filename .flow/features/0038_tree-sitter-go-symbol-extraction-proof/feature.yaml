schema: flow-feature.v1
feature:
  id: "0038"
  slug: tree-sitter-go-symbol-extraction-proof
  title: Tree-sitter Go symbol extraction proof
  lane: standard
  status: done
  autonomy_default: ask
intent:
  problem: The Go parser now compiles and parses through an isolated proof, but Go
    symbol extraction semantics, ranges, ordering, mapping, and degraded states
    are not yet proven.
  outcome: Add an internal non-product Tree-sitter Go symbol extraction proof that
    maps a bounded Go symbol subset into the existing provider symbol evidence
    shape while preserving all current CLI and report behaviour.
  acceptance:
    - Dedicated command `zig build tree-sitter-go-symbol-proof` exists and
      passes.
    - The proof uses vendored local Tree-sitter core and tree-sitter-go parser
      sources only.
    - The proof uses direct AST traversal first; no Tree-sitter query files or
      custom query execution are added.
    - The proof extracts package, top-level functions, methods, struct and
      interface type specs, and top-level const/var names when feasible.
    - Symbol ranges use deterministic one-based inclusive line ranges from the
      enclosing Go declaration node.
    - Symbol ordering is deterministic and source-order based for the proof.
    - Symbol kind mapping stays within the existing provider model and uses
      caveats for structs, interfaces, and constants where needed.
    - Empty, invalid, unsupported, unsafe-path, generated-file, build-tag, and
      cgo-adjacent cases are covered or caveated without raw diagnostics.
    - Existing Zig and Go build proof targets still pass.
    - No user-facing Go symbols, provider registry, CLI/report/schema/scoring,
      cache, network, telemetry, or parser-generation behaviour is added.
    - No-provider outputs and existing Zig symbol outputs remain stable.
    - Public evidence documents supported subset, range/order semantics,
      validation, and that Go runtime symbol output is not implemented yet.
planning:
  state: ready
  summary: Prove Go symbol extraction semantics internally before exposing Go
    symbols to users. This mirrors the Zig sequence of source import, build
    proof, internal extraction proof, then user-facing inspect output.
  brief_sync:
    status: updated
    reason: Supports B001 provider-as-optional-enricher direction while keeping
      file-level Git evidence as product truth.
    checked_at: 2026-05-24T15:30:00.000Z
  brief_alignment:
    status: aligned
    role: follow_up
    summary: This is a prerequisite provider implementation slice for the B001
      local-first hotspot intelligence capability; it adds no product-facing Go
      output.
    checked_at: 2026-05-24T15:30:00.000Z
proposal:
  lifecycle_state: shaping
  outcome_contract_ref: null
  risk_assessment:
    assurance_level: high_assurance
    reasons:
      - topology=serial_feature
      - assurance=high_assurance
      - review_mode=delegated_reviewer
      - runner_model=Strong
    risk_triggers:
      - standard_lane_delivery
      - feature_dependencies_present
      - packet_verification_commands_missing
      - public_api_or_platform_risk_keywords
    audit_required: true
    judge_review_recommended: true
    materialised_at: 2026-05-24T15:23:40.102Z
  quality_plan: null
project_feature:
  lifecycle_state: done
  outcome_contract_ref:
    contract_kind: feature_intent
    contract_ref: feature.intent
    materialised_at: 2026-05-24T15:23:40.102Z
  quality_plan:
    assurance_band: high_assurance
    review_mode: delegated_reviewer
    reviewer_member_usage: required
    reviewer_expectations:
      - verify acceptance against dispatch comparison basis
      - treat runner evidence as untrusted until independently checked
    verification_contract:
      claim_classes: []
      evidence_policy:
        parity_claims: null
        synthetic_only_is_insufficient: false
      route_matrix:
        required: false
        routes: []
      resolution_matrix:
        required: false
        cases: []
      operator_examples:
        required: false
        cases: []
    derived_from_risk_assessment_ref: proposal.risk_assessment
    materialised_at: 2026-05-24T15:23:40.102Z
  authority_refinement:
    selected_sign_off_authority: orchestrator
    explicit_pause_required: true
    verification_independence_requirement: continuity_safe_delegated
    materialised_at: 2026-05-24T15:23:40.102Z
verification_independence_requirement: continuity_safe_delegated
readiness:
  requirements_ready: true
  acceptance_ready: true
  packet_plan_ready: true
  dependencies_ready: true
  implementation_ready: true
context:
  roadmap_item_id: null
  detached_reason: Follow-up to Feature 0037, which proved tree-sitter-go compile,
    link, and parse behaviour while explicitly deferring Go symbol extraction
    and user-facing provider output.
  depends_on_feature_ids:
    - "0037"
  blocked_by: []
  linked_brief_ids:
    - B001
design:
  rough_shape:
    - Add an isolated `zig build tree-sitter-go-symbol-proof` target.
    - Use direct AST traversal over in-memory Go fixtures.
    - Extract a bounded top-level symbol subset into provider-compatible
      current-symbol evidence.
    - Prove deterministic range, order, caveat, and failure semantics.
    - Preserve no-provider output stability and existing Zig symbol behaviour.
    - Explicitly defer Go user-facing symbol output to a later feature.
  packet_contract:
    version: v1
    review_sequence:
      - spec_compliance
      - code_quality
    verification_required: false
  packets:
    - id: P1
      title: Internal Go extraction proof
      goal: Add a non-product Go symbol extraction proof target that extracts a
        bounded Go symbol subset from in-memory fixtures using vendored local
        Tree-sitter sources and direct AST traversal.
      owner_phase: runner
      depends_on: []
      status: done
      execution_context:
        - "Anchors: build.zig, tests/tree_sitter_go_symbol_proof.zig or
          equivalent, src/provider.zig, third_party/tree-sitter-go/v0.25.0,
          third_party/tree-sitter-core/v0.26.9"
        - "In scope: add `zig build tree-sitter-go-symbol-proof`; compile/link
          local core lib.c and Go parser.c for proof; extract package, top-level
          functions, methods, struct/interface type specs, top-level const/var
          names when feasible; use provider-compatible current-symbol evidence"
        - "Out of scope: user-facing Go `--symbols`,
          CLI/report/schema/scoring/cache changes, provider registry, Go
          package/module analysis, custom queries, LSP, parser generation,
          network/package-manager/global Tree-sitter input"
        - "Protected: installed product CLI runtime, existing no-provider
          outputs, existing Zig symbol outputs, provider public semantics,
          file-level Git evidence as product truth"
        - "Dependencies: Feature 0037 Go offline build proof and existing
          provider.CurrentSymbolEvidence seam"
        - "Validation/evidence: zig fmt --check build.zig src tests; zig build
          test; zig build; zig build validate; zig build
          tree-sitter-build-proof; zig build tree-sitter-symbol-proof; zig build
          tree-sitter-go-build-proof; zig build tree-sitter-go-symbol-proof; git
          diff --check"
        - "Review focus: proof remains internal/non-product; direct traversal
          semantics are explicit; mapping/range/order/caveats are deterministic;
          no runtime/report drift"
        - "Escalate if: Go symbol subset requires provider enum expansion,
          receiver-qualified naming contract, query framework, parser
          generation, package-manager input, or user-facing output"
        - "Done boundary: proof target passes; supported subset and edge cases
          are tested; no-provider and Zig symbol outputs remain stable"
      anchors:
        - build.zig
        - tests/tree_sitter_go_symbol_proof.zig
        - src/provider.zig
        - third_party/tree-sitter-go/v0.25.0/
        - third_party/tree-sitter-core/v0.26.9/
      in_scope:
        - Dedicated internal Go symbol proof target.
        - Direct AST traversal over in-memory Go fixtures.
        - Provider-compatible current-symbol evidence.
        - Deterministic range and order semantics.
      out_of_scope:
        - User-facing Go symbols.
        - CLI, report, schema, scoring, cache, runtime provider, registry, CI
          release, package, LSP, or parser-generation changes.
      protected_surfaces:
        - src/main.zig user-facing routes
        - src/report.zig report schema and renderers except tests if needed
        - fixtures/expected/**
        - product CLI/report/scoring/cache behaviour
      validation_commands:
        - zig fmt --check build.zig src tests
        - zig build test
        - zig build
        - zig build validate
        - zig build tree-sitter-build-proof
        - zig build tree-sitter-symbol-proof
        - zig build tree-sitter-go-build-proof
        - zig build tree-sitter-go-symbol-proof
        - git diff --check
      review_focus:
        - Go extraction semantics are deterministic and bounded.
        - Existing provider shape is used honestly.
        - No user-facing Go output or report drift was introduced.
      escalation_triggers:
        - Required symbol kinds cannot be represented without changing the
          provider enum.
        - Method receiver naming requires a new public contract.
        - Direct traversal cannot prove deterministic semantics.
        - Proof requires network, package manager, global Tree-sitter, or parser
          generation.
      done_boundary: Internal Go symbol proof passes and existing validation remains
        green with no runtime/provider/report drift.
    - id: P2
      title: Evidence docs and close-out proof
      goal: Record public, privacy-safe evidence for the Go extraction proof,
        supported subset, range/order semantics, validation commands, and the
        explicit non-runtime boundary.
      owner_phase: runner
      depends_on:
        - P1
      status: done
      execution_context:
        - "Anchors: docs/tree-sitter-go-symbol-extraction-proof.md, build.zig,
          tests/tree_sitter_go_symbol_proof.zig"
        - "In scope: record supported subset, direct traversal boundary,
          range/order semantics, symbol-kind mapping, caveats, validation
          commands, local/offline proof, and no-runtime Go support statement"
        - "Out of scope: raw sibling paths/output, private repo details, parser
          stderr dumps, source snippets from private repos, Go runtime support
          claims"
        - "Protected: OSS/publicity framing, local-first defaults, no bug
          prediction/code-quality/developer-ranking claims"
        - "Dependencies: P1 proof target must pass before evidence is final"
        - "Validation/evidence: changed-path scan; prohibited dependency scan;
          privacy/prohibited-claim scan; close-out smoke with sibling-local-repo
          label; Flow validation"
        - "Review focus: evidence is concise, public-safe, repo-relative, and
          does not imply Go user-facing provider support is implemented"
        - "Escalate if: evidence needs private data, no-provider output drift
          appears, or docs imply Go symbols are available in CLI output"
        - "Done boundary: evidence doc complete; close-out rows are
          independently reviewable; feature ready for delegated review"
      anchors:
        - docs/tree-sitter-go-symbol-extraction-proof.md
        - build.zig
        - tests/tree_sitter_go_symbol_proof.zig
      in_scope:
        - Public proof evidence document.
        - Privacy-safe validation and close-out summaries.
        - Explicit deferral of user-facing Go symbols.
      out_of_scope:
        - Raw private output or paths.
        - Runtime Go support claims.
      protected_surfaces:
        - README public claims unless a narrow correction is required.
        - CONTRIBUTING and CI behaviour unless proof target is intentionally
          added.
      validation_commands:
        - git diff --check
        - zig build validate
        - zig build tree-sitter-go-symbol-proof
        - zig build validate -Dcloseout=true -Dsmoke-repo=<local-sibling-path>
          -Dsmoke-label=sibling-local-repo
      review_focus:
        - Evidence proves the internal proof and does not overclaim product
          support.
        - Sibling evidence is label-only.
      escalation_triggers:
        - Docs need to claim Go CLI support.
        - Evidence requires private or raw parser output.
        - CI inclusion decision changes validation cost materially.
      done_boundary: Evidence and close-out validation are complete, public-safe, and
        independently reviewable.
links:
  brief_ids:
    - B001
execution:
  requires_confirmation: true
  blockers: []
  active_run_id: null
  current_packet_id: null
  next_packet_ids: []
timestamps:
  created_at: 2026-05-24T15:17:08.305Z
  updated_at: 2026-05-24T15:54:09.788Z
dispatch:
  status: prepared
  prepared_at: 2026-05-24T15:28:37.003Z
  assurance_level: high_assurance
  control_model: split
  execution_topology: serial_feature
  model_plan:
    control_plane_model: Strong
    runner_model: Strong
    review_mode: delegated_reviewer
    review_model: Strong
    reviewer_member_usage: required
    research_model: null
    scaffold_owner_model: null
    fill_worker_model: null
    escalation_triggers:
      - repeated validation failures
      - dispatch envelope no longer fits implementation reality
      - public interface, persistence, security, or concurrency change
  execution_fit:
    fast_execute_eligible: false
    reason: Assurance level high_assurance requires stronger than Fast execution.
  scope:
    mode: packets
    feature_id: "0038"
    packet_ids:
      - P1
      - P2
  comparison_basis:
    roadmap_item_id: null
    linked_brief_ids:
      - B001
    feature:
      title: Tree-sitter Go symbol extraction proof
      problem: The Go parser now compiles and parses through an isolated proof, but Go
        symbol extraction semantics, ranges, ordering, mapping, and degraded
        states are not yet proven.
      outcome: Add an internal non-product Tree-sitter Go symbol extraction proof that
        maps a bounded Go symbol subset into the existing provider symbol
        evidence shape while preserving all current CLI and report behaviour.
      acceptance:
        - Dedicated command `zig build tree-sitter-go-symbol-proof` exists and
          passes.
        - The proof uses vendored local Tree-sitter core and tree-sitter-go
          parser sources only.
        - The proof uses direct AST traversal first; no Tree-sitter query files
          or custom query execution are added.
        - The proof extracts package, top-level functions, methods, struct and
          interface type specs, and top-level const/var names when feasible.
        - Symbol ranges use deterministic one-based inclusive line ranges from
          the enclosing Go declaration node.
        - Symbol ordering is deterministic and source-order based for the proof.
        - Symbol kind mapping stays within the existing provider model and uses
          caveats for structs, interfaces, and constants where needed.
        - Empty, invalid, unsupported, unsafe-path, generated-file, build-tag,
          and cgo-adjacent cases are covered or caveated without raw
          diagnostics.
        - Existing Zig and Go build proof targets still pass.
        - No user-facing Go symbols, provider registry,
          CLI/report/schema/scoring, cache, network, telemetry, or
          parser-generation behaviour is added.
        - No-provider outputs and existing Zig symbol outputs remain stable.
        - Public evidence documents supported subset, range/order semantics,
          validation, and that Go runtime symbol output is not implemented yet.
    brief_alignment:
      status: aligned
      role: follow_up
      summary: This is a prerequisite provider implementation slice for the B001
        local-first hotspot intelligence capability; it adds no product-facing
        Go output.
      checked_at: 2026-05-24T15:30:00.000Z
    briefs:
      - id: B001
        title: Local-first hotspot intelligence CLI
        summary: "Define the OSS capability for git-hotspots as a deterministic,
          local-first hotspot intelligence CLI for developers and coding agents:
          before refactoring, ask Git where change pressure and maintenance pain
          concentrate."
        desired_capability:
          - A command-line tool that analyses local Git history and produces
            reproducible hotspot evidence for files first, with symbol-level
            insight added later through provider adapters.
          - Reports should help humans and coding agents focus review,
            refactoring, testing, and onboarding effort without treating metrics
            as runtime truth or quality judgement.
          - Provider architecture should allow future evidence sources such as
            tree-sitter, LSP, ctags, dependency graphs, blame/history, and test
            or coverage data without making any single provider the core
            product.
        why:
          - "High change frequency is a useful attention signal: code that
            changes often may be fragile, strategically important, under-tested,
            or worth improving."
          - The reboot should avoid the old parser-heavy trap and prove
            usefulness with deterministic Git-history evidence before deep
            language support.
          - Local-first reports preserve trust and make the project suitable for
            private repositories, OSS maintainers, and agent-assisted workflows.
        in_scope:
          - OSS/publicity-oriented CLI capability and public project framing.
          - "File-level hotspot analysis as the first useful layer: churn,
            change frequency, recency, co-change, size, confidence, and
            explainable evidence."
          - Agent-ready export formats such as JSON and Markdown that users may
            pipe into their own tools or agents.
          - First-class provider concept for later symbol and dependency
            evidence, with tree-sitter as one provider rather than the
            foundation.
          - "Campaign language such as: Your git history knows where your
            technical debt lives; Stop refactoring by vibes; Before asking an AI
            to refactor, ask Git where to look; The riskiest code is not always
            the ugliest code, it is the code everyone keeps changing."
        out_of_scope:
          - Hosted dashboards, SaaS, and commercial or monetisation plans in
            repository artefacts.
          - Claims that the tool predicts bugs, measures code quality, ranks
            developers, or replaces established code-health platforms.
          - Mandatory LLM use at runtime; agents may consume exports, but the
            product truth remains deterministic.
          - Making tree-sitter, LSP, or any language provider part of the
            initial critical path.
          - Feature-level execution plans, PRDs, packet plans, todos, or runs
            during this brief-capture step.
        durable_decisions:
          - "Adopt the public thesis: deterministic, local-first hotspot
            intelligence CLI for developers and coding agents."
          - "Adopt the tagline: Before you refactor, ask Git where the pain is."
          - Start conceptually with file-level Git-history evidence; defer
            symbol-level precision until provider seams are validated.
          - Treat tree-sitter as a later provider, alongside possible LSP,
            ctags, dependency, blame/history, and test/coverage providers.
          - Keep the public repository free of monetisation plans and SaaS
            strategy; use it as OSS/publicity framing only.
          - Use agent-ready reports as an export surface, not as model-driven
            product truth.
    prd_path: .flow/features/0038_tree-sitter-go-symbol-extraction-proof/prd.md
  requirements_contract:
    source_path: .flow/features/0038_tree-sitter-go-symbol-extraction-proof/prd.md
    source_digest: a7ea1d3dfeb4b805d71f816cd62ebfba47e5e5e50a8cef81d22c8d40e3da246d
    extraction: structured_markdown
    requirements:
      - id: REQ-001
        source_ref: prd.md#non-goals[1]
        text: No user-facing Go --symbols output.
        category: non_goal
        packet_refs: []
        verification: null
        required_for_close_out: false
      - id: REQ-002
        source_ref: prd.md#non-goals[2]
        text: No CLI flag changes.
        category: non_goal
        packet_refs: []
        verification: null
        required_for_close_out: false
      - id: REQ-003
        source_ref: prd.md#non-goals[3]
        text: No report schema, fixture output, scoring, ranking, lineage, cache, or CI
          release changes.
        category: non_goal
        packet_refs: []
        verification: null
        required_for_close_out: false
      - id: REQ-004
        source_ref: prd.md#non-goals[4]
        text: No Go provider registry entry.
        category: non_goal
        packet_refs: []
        verification: null
        required_for_close_out: false
      - id: REQ-005
        source_ref: prd.md#non-goals[5]
        text: No Go package-manager, module, build-tag evaluation, cgo analysis,
          repo-wide scanning, LSP, custom query execution, or dependency graph
          analysis.
        category: non_goal
        packet_refs: []
        verification: null
        required_for_close_out: false
      - id: REQ-006
        source_ref: prd.md#non-goals[6]
        text: No git log -L, Go symbol history, symbol lineage, source snippets, raw
          parser diagnostics, authors, emails, commit messages, ownership
          metrics, bug prediction, quality scoring, or developer ranking.
        category: non_goal
        packet_refs: []
        verification: null
        required_for_close_out: false
      - id: REQ-007
        source_ref: prd.md#non-goals[7]
        text: No parser generation, network fetches, package-manager resolution,
          submodules, global Tree-sitter CLI, system parser packages, or
          build.zig.zon.
        category: non_goal
        packet_refs: []
        verification: null
        required_for_close_out: false
      - id: REQ-008
        source_ref: prd.md#r8-existing-behaviour-preservation[1]
        text: "Existing proof targets and product validation must continue to pass:"
        category: behaviour
        packet_refs: []
        verification: null
        required_for_close_out: true
      - id: REQ-009
        source_ref: prd.md#r8-existing-behaviour-preservation[2]
        text: "``sh zig build tree-sitter-build-proof zig build tree-sitter-symbol-proof
          zig build tree-sitter-go-build-proof zig build validate ``"
        category: behaviour
        packet_refs:
          - P1
        verification: null
        required_for_close_out: true
      - id: REQ-010
        source_ref: prd.md#r8-existing-behaviour-preservation[3]
        text: No-provider table, JSON, Markdown, inspect, and existing Zig --symbols
          outputs must remain byte-stable unless a separate feature explicitly
          changes them.
        category: behaviour
        packet_refs: []
        verification: null
        required_for_close_out: true
      - id: REQ-011
        source_ref: prd.md#r9-evidence-document[1]
        text: "Add or update a concise public evidence document, expected path:"
        category: verification
        packet_refs: []
        verification: "Add or update a concise public evidence document, expected path:"
        required_for_close_out: true
      - id: REQ-012
        source_ref: prd.md#r9-evidence-document[2]
        text: "``text docs/tree-sitter-go-symbol-extraction-proof.md ``"
        category: verification
        packet_refs:
          - P1
        verification: "``text docs/tree-sitter-go-symbol-extraction-proof.md ``"
        required_for_close_out: true
      - id: REQ-013
        source_ref: prd.md#r9-evidence-document[3]
        text: "It must record:"
        category: verification
        packet_refs: []
        verification: "It must record:"
        required_for_close_out: true
      - id: REQ-014
        source_ref: prd.md#r9-evidence-document[4]
        text: proof target name;
        category: verification
        packet_refs: []
        verification: proof target name;
        required_for_close_out: true
      - id: REQ-015
        source_ref: prd.md#r9-evidence-document[5]
        text: supported Go subset;
        category: verification
        packet_refs: []
        verification: supported Go subset;
        required_for_close_out: true
      - id: REQ-016
        source_ref: prd.md#r9-evidence-document[6]
        text: direct traversal boundary;
        category: verification
        packet_refs:
          - P1
        verification: direct traversal boundary;
        required_for_close_out: true
      - id: REQ-017
        source_ref: prd.md#r9-evidence-document[7]
        text: range and ordering semantics;
        category: verification
        packet_refs:
          - P2
        verification: range and ordering semantics;
        required_for_close_out: true
      - id: REQ-018
        source_ref: prd.md#r9-evidence-document[8]
        text: symbol-kind mapping and caveats;
        category: verification
        packet_refs: []
        verification: symbol-kind mapping and caveats;
        required_for_close_out: true
      - id: REQ-019
        source_ref: prd.md#r9-evidence-document[9]
        text: validation commands and exit statuses;
        category: verification
        packet_refs:
          - P2
        verification: validation commands and exit statuses;
        required_for_close_out: true
      - id: REQ-020
        source_ref: prd.md#r9-evidence-document[10]
        text: local/offline proof boundary;
        category: verification
        packet_refs: []
        verification: local/offline proof boundary;
        required_for_close_out: true
      - id: REQ-021
        source_ref: prd.md#r9-evidence-document[11]
        text: explicit statement that Go runtime --symbols output is not implemented
          yet.
        category: verification
        packet_refs: []
        verification: explicit statement that Go runtime --symbols output is not
          implemented yet.
        required_for_close_out: true
      - id: REQ-022
        source_ref: prd.md#r10-ci-decision[1]
        text: The new proof target must either be added to CI after existing proof steps
          or a durable reason must be recorded for excluding it. If added, CI
          must remain local after checkout and Zig setup and must not require
          sibling repo smoke, secrets, artifacts, release automation, cache,
          package managers, or network fetches.
        category: constraint
        packet_refs: []
        verification: null
        required_for_close_out: true
      - id: REQ-023
        source_ref: prd.md#edge-cases[1]
        text: If the Go parser API or node names differ from the expected source-import
          evidence, stop and record a blocker instead of broadening extraction
          locally.
        category: edge_case
        packet_refs: []
        verification: null
        required_for_close_out: true
      - id: REQ-024
        source_ref: prd.md#edge-cases[2]
        text: If receiver-qualified method names are needed to avoid ambiguity, stop and
          shape that naming contract separately. The first proof may use bare
          method names with a caveat.
        category: edge_case
        packet_refs: []
        verification: null
        required_for_close_out: true
      - id: REQ-025
        source_ref: prd.md#edge-cases[3]
        text: If structs, interfaces, constants, or variables cannot be mapped without
          expanding provider.SymbolKind, either map conservatively with caveats
          or stop for a seam-extension feature.
        category: edge_case
        packet_refs: []
        verification: null
        required_for_close_out: true
      - id: REQ-026
        source_ref: prd.md#edge-cases[4]
        text: If partial parse behaviour leaks diagnostics or snippets, block close-out.
        category: edge_case
        packet_refs: []
        verification: null
        required_for_close_out: true
      - id: REQ-027
        source_ref: prd.md#edge-cases[5]
        text: If no-provider outputs or existing Zig symbol outputs change, block
          close-out.
        category: edge_case
        packet_refs: []
        verification: null
        required_for_close_out: true
      - id: REQ-028
        source_ref: prd.md#edge-cases[6]
        text: If CI inclusion creates material cost or platform instability, record the
          decision and stop for planning.
        category: edge_case
        packet_refs: []
        verification: null
        required_for_close_out: true
      - id: REQ-029
        source_ref: prd.md#verification[1]
        text: "Close-out must include evidence for:"
        category: verification
        packet_refs:
          - P2
        verification: "Close-out must include evidence for:"
        required_for_close_out: true
      - id: REQ-030
        source_ref: prd.md#verification[2]
        text: "``sh zig fmt --check build.zig src tests zig build test zig build zig
          build validate zig build tree-sitter-build-proof zig build
          tree-sitter-symbol-proof zig build tree-sitter-go-build-proof zig
          build tree-sitter-go-symbol-proof git diff --check zig build validate
          -Dcloseout=true -Dsmoke-repo=<local-sibling-path>
          -Dsmoke-label=sibling-local-repo ``"
        category: verification
        packet_refs:
          - P1
        verification: "``sh zig fmt --check build.zig src tests zig build test zig build
          zig build validate zig build tree-sitter-build-proof zig build
          tree-sitter-symbol-proof zig build tree-sitter-go-build-proof zig
          build tree-sitter-go-symbol-proof git diff --check zig build validate
          -Dcloseout=true -Dsmoke-repo=<local-sibling-path>
          -Dsmoke-label=sibling-local-repo ``"
        required_for_close_out: true
      - id: REQ-031
        source_ref: prd.md#verification[3]
        text: The sibling path is execution-only context and must not be committed.
          Durable summary evidence must use only the label sibling-local-repo.
        category: verification
        packet_refs: []
        verification: The sibling path is execution-only context and must not be
          committed. Durable summary evidence must use only the label
          sibling-local-repo.
        required_for_close_out: true
    supporting_context:
      - source_ref: prd.md#problem[1]
        text: "Feature 0036 imported pinned tree-sitter-go sources and Feature 0037
          proved that the parser compiles, links, and parses a tiny Go snippet
          with the existing vendored Tree-sitter core. The next risk is symbol
          semantics: which Go nodes we extract, how we map them into the
          existing provider model, how ranges and order are made deterministic,
          and how unsupported or degraded cases are caveated."
      - source_ref: prd.md#problem[2]
        text: Without an internal extraction proof, user-facing Go --symbols output
          would combine extraction semantics, CLI/report UX, and validation risk
          in one step.
    notes:
      - Extracted requirements from PRD requirement, acceptance, behaviour,
        constraint, edge-case, verification, or case sections.
      - Supporting context and structural headings are not close-out required;
        kept 2 refs outside default requirement rows.
  acceptance_contract:
    outcome: Add an internal non-product Tree-sitter Go symbol extraction proof that
      maps a bounded Go symbol subset into the existing provider symbol evidence
      shape while preserving all current CLI and report behaviour.
    acceptance:
      - Dedicated command `zig build tree-sitter-go-symbol-proof` exists and
        passes.
      - The proof uses vendored local Tree-sitter core and tree-sitter-go parser
        sources only.
      - The proof uses direct AST traversal first; no Tree-sitter query files or
        custom query execution are added.
      - The proof extracts package, top-level functions, methods, struct and
        interface type specs, and top-level const/var names when feasible.
      - Symbol ranges use deterministic one-based inclusive line ranges from the
        enclosing Go declaration node.
      - Symbol ordering is deterministic and source-order based for the proof.
      - Symbol kind mapping stays within the existing provider model and uses
        caveats for structs, interfaces, and constants where needed.
      - Empty, invalid, unsupported, unsafe-path, generated-file, build-tag, and
        cgo-adjacent cases are covered or caveated without raw diagnostics.
      - Existing Zig and Go build proof targets still pass.
      - No user-facing Go symbols, provider registry, CLI/report/schema/scoring,
        cache, network, telemetry, or parser-generation behaviour is added.
      - No-provider outputs and existing Zig symbol outputs remain stable.
      - Public evidence documents supported subset, range/order semantics,
        validation, and that Go runtime symbol output is not implemented yet.
  validation_contract:
    ladders:
      - V2_atomic_commit
      - V3_final_packet
      - V4_close_out
    review_sequence:
      - spec_compliance
      - code_quality
    packet_verification_commands: []
    reviewer_must_repeat: true
    claim_classes: []
    evidence_policy:
      parity_claims: null
      synthetic_only_is_insufficient: false
    route_matrix:
      required: false
      routes: []
    resolution_matrix:
      required: false
      cases: []
    operator_examples:
      required: false
      cases: []
  ownership:
    reviewer_expectations:
      - verify acceptance against dispatch comparison basis
      - treat runner evidence as untrusted until independently checked
  decision_budget:
    allowed_local_decisions:
      - safe internal refactor inside dispatched scope
      - local implementation detail changes that do not alter contracts
    deferred_reconcile_triggers:
      - minor non-contract deviation worth reviewer visibility
      - implementation trade-off that stays inside acceptance
    hard_planner_escalation_triggers:
      - scope or acceptance change
      - dependency or prerequisite change
      - protected-surface mutation outside dispatch envelope
    protected_surfaces: []
  fallback_path: deferred_reconcile_decision
  notes: null
