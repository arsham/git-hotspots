schema: flow-feature.v1
feature:
  id: "0030"
  slug: ci-validation-workflow
  title: CI validation workflow
  lane: standard
  status: ready
  autonomy_default: ask
intent:
  problem: The project now has non-trivial Zig validation, vendored Tree-sitter
    sources, and inspect-only symbol evidence, but the public repository has no
    CI signal that the validation contract passes for contributors or publicity
    readers.
  outcome: Add a minimal GitHub Actions workflow that runs the public validation
    gate on push and pull request without release automation, uploads, cache,
    telemetry, or product behaviour changes.
  acceptance:
    - A single GitHub Actions workflow validates the project on push and pull
      request.
    - The workflow uses full checkout history because git-hotspots is
      Git-history-sensitive.
    - The workflow uses Zig 0.16.0.
    - The workflow runs `zig build validate`.
    - The workflow runs `zig build tree-sitter-build-proof` and `zig build
      tree-sitter-symbol-proof`.
    - The workflow has least-privilege read-only repository permissions.
    - The workflow contains no release, package, artifact-upload, cache,
      coverage, telemetry, secret, or publish steps.
    - Local validation remains green after adding the workflow.
planning:
  state: ready
  summary: Minimal public CI validation only. CI proves the public/default
    validation and explicit Tree-sitter proof steps; sibling/local close-out
    smoke remains a local operator gate.
  brief_sync:
    status: updated
    role: derived
    notes: CI supports B001's OSS/publicity and local-first validation posture
      without changing product behaviour. No brief text change is required.
    checked_at: 2026-05-23T22:30:00Z
  brief_alignment:
    status: aligned
    role: follow_up
    summary: CI is a follow-up validation capability for the public OSS CLI and does
      not change B001's product thesis or runtime behaviour.
    notes: Supports public confidence and contributor validation while preserving
      deterministic local-first runtime constraints and avoiding release or
      hosted product scope.
    checked_at: 2026-05-23T22:30:00Z
proposal:
  lifecycle_state: shaping
  outcome_contract_ref: null
  risk_assessment:
    assurance_level: high_assurance
    reasons:
      - topology=serial_feature
      - assurance=high_assurance
      - review_mode=delegated_reviewer
      - runner_model=Strong
    risk_triggers:
      - standard_lane_delivery
      - feature_dependencies_present
      - public_api_or_platform_risk_keywords
    audit_required: true
    judge_review_recommended: true
    materialised_at: 2026-05-23T22:28:11.793Z
  quality_plan: null
project_feature:
  lifecycle_state: ready
  outcome_contract_ref:
    contract_kind: feature_intent
    contract_ref: feature.intent
    materialised_at: 2026-05-23T22:28:11.793Z
  quality_plan:
    assurance_band: high_assurance
    review_mode: delegated_reviewer
    reviewer_member_usage: required
    reviewer_expectations:
      - verify acceptance against dispatch comparison basis
      - treat runner evidence as untrusted until independently checked
    verification_contract:
      claim_classes: []
      evidence_policy:
        parity_claims: null
        synthetic_only_is_insufficient: false
      route_matrix:
        required: false
        routes: []
      resolution_matrix:
        required: false
        cases: []
      operator_examples:
        required: false
        cases: []
    derived_from_risk_assessment_ref: proposal.risk_assessment
    materialised_at: 2026-05-23T22:28:11.793Z
  authority_refinement:
    selected_sign_off_authority: orchestrator
    explicit_pause_required: true
    verification_independence_requirement: continuity_safe_delegated
    materialised_at: 2026-05-23T22:28:11.793Z
verification_independence_requirement: continuity_safe_delegated
readiness:
  requirements_ready: true
  acceptance_ready: true
  packet_plan_ready: true
  dependencies_ready: true
  implementation_ready: true
context:
  roadmap_item_id: null
  detached_reason: Next logical public validation slice after symbol line-history work.
  depends_on_feature_ids:
    - "0029"
  blocked_by: []
design:
  rough_shape:
    - Add `.github/workflows/ci.yml` or equivalent minimal validation workflow.
    - Use checkout with full history and Zig 0.16.0.
    - Run canonical validation and explicit Tree-sitter proof steps.
    - Avoid all release/publish/upload/cache/matrix scope.
  packets:
    - id: P1
      title: Minimal GitHub Actions validation workflow
      goal: Add one minimal GitHub Actions workflow for public validation.
      status: planned
      depends_on: []
      owner_phase: runner
      execution_context:
        - "Anchors: `.github/workflows/ci.yml`, `build.zig`,
          `tools/validate.sh`, `README.md`, `CONTRIBUTING.md`."
        - "In scope: add one workflow on push/pull_request that checks out full
          history, uses Zig 0.16.0, and runs validation/proof commands."
        - "Out of scope: release automation, packaging, artifacts, cache,
          coverage, matrix, badges, secrets, telemetry, runtime behaviour
          changes."
        - "Protected: local-first runtime defaults, public OSS framing, existing
          validation semantics, no sibling/local repo data in CI."
        - "Dependencies: existing `zig build validate`,
          `tree-sitter-build-proof`, and `tree-sitter-symbol-proof` steps."
        - "Validation/evidence: run `git diff --check`, `zig build validate`,
          `zig build tree-sitter-build-proof`, `zig build
          tree-sitter-symbol-proof`; inspect workflow YAML for scope and
          permissions."
        - "Review focus: minimal triggers, full checkout history, Zig 0.16.0,
          read-only permissions, no prohibited CI behaviours."
        - "Escalate if: Zig setup requires an unacceptable unpinned third-party
          action, CI needs secrets, validation is too slow/flaky, or workflow
          scope expands."
        - "Done boundary: workflow exists, local gates pass, and reviewer can
          prove no prohibited CI scope was added."
      verification_commands:
        - git diff --check
        - zig build validate
        - zig build tree-sitter-build-proof
        - zig build tree-sitter-symbol-proof
  packet_contract:
    version: v1
    review_sequence:
      - spec_compliance
      - code_quality
    verification_required: false
execution:
  active_run_id: null
  current_packet_id: null
  next_packet_ids:
    - P1
  blockers: []
  requires_confirmation: true
links:
  epic_todo_id: null
  brief_ids:
    - B001
timestamps:
  created_at: 2026-05-23T22:22:04.682Z
  updated_at: 2026-05-23T22:29:23.361Z
dispatch:
  status: prepared
  prepared_at: 2026-05-23T22:29:23.361Z
  assurance_level: high_assurance
  control_model: split
  execution_topology: serial_feature
  model_plan:
    control_plane_model: Strong
    runner_model: Strong
    review_mode: delegated_reviewer
    review_model: Strong
    reviewer_member_usage: required
    research_model: null
    scaffold_owner_model: null
    fill_worker_model: null
    escalation_triggers:
      - repeated validation failures
      - dispatch envelope no longer fits implementation reality
      - public interface, persistence, security, or concurrency change
  execution_fit:
    fast_execute_eligible: false
    reason: Assurance level high_assurance requires stronger than Fast execution.
  scope:
    mode: packets
    feature_id: "0030"
    packet_ids:
      - P1
  comparison_basis:
    roadmap_item_id: null
    linked_brief_ids:
      - B001
    feature:
      title: CI validation workflow
      problem: The project now has non-trivial Zig validation, vendored Tree-sitter
        sources, and inspect-only symbol evidence, but the public repository has
        no CI signal that the validation contract passes for contributors or
        publicity readers.
      outcome: Add a minimal GitHub Actions workflow that runs the public validation
        gate on push and pull request without release automation, uploads,
        cache, telemetry, or product behaviour changes.
      acceptance:
        - A single GitHub Actions workflow validates the project on push and
          pull request.
        - The workflow uses full checkout history because git-hotspots is
          Git-history-sensitive.
        - The workflow uses Zig 0.16.0.
        - The workflow runs `zig build validate`.
        - The workflow runs `zig build tree-sitter-build-proof` and `zig build
          tree-sitter-symbol-proof`.
        - The workflow has least-privilege read-only repository permissions.
        - The workflow contains no release, package, artifact-upload, cache,
          coverage, telemetry, secret, or publish steps.
        - Local validation remains green after adding the workflow.
    brief_alignment:
      status: aligned
      role: follow_up
      summary: CI is a follow-up validation capability for the public OSS CLI and does
        not change B001's product thesis or runtime behaviour.
      checked_at: 2026-05-23T22:30:00Z
    briefs:
      - id: B001
        title: Local-first hotspot intelligence CLI
        summary: "Define the OSS capability for git-hotspots as a deterministic,
          local-first hotspot intelligence CLI for developers and coding agents:
          before refactoring, ask Git where change pressure and maintenance pain
          concentrate."
        desired_capability:
          - A command-line tool that analyses local Git history and produces
            reproducible hotspot evidence for files first, with symbol-level
            insight added later through provider adapters.
          - Reports should help humans and coding agents focus review,
            refactoring, testing, and onboarding effort without treating metrics
            as runtime truth or quality judgement.
          - Provider architecture should allow future evidence sources such as
            tree-sitter, LSP, ctags, dependency graphs, blame/history, and test
            or coverage data without making any single provider the core
            product.
        why:
          - "High change frequency is a useful attention signal: code that
            changes often may be fragile, strategically important, under-tested,
            or worth improving."
          - The reboot should avoid the old parser-heavy trap and prove
            usefulness with deterministic Git-history evidence before deep
            language support.
          - Local-first reports preserve trust and make the project suitable for
            private repositories, OSS maintainers, and agent-assisted workflows.
        in_scope:
          - OSS/publicity-oriented CLI capability and public project framing.
          - "File-level hotspot analysis as the first useful layer: churn,
            change frequency, recency, co-change, size, confidence, and
            explainable evidence."
          - Agent-ready export formats such as JSON and Markdown that users may
            pipe into their own tools or agents.
          - First-class provider concept for later symbol and dependency
            evidence, with tree-sitter as one provider rather than the
            foundation.
          - "Campaign language such as: Your git history knows where your
            technical debt lives; Stop refactoring by vibes; Before asking an AI
            to refactor, ask Git where to look; The riskiest code is not always
            the ugliest code, it is the code everyone keeps changing."
        out_of_scope:
          - Hosted dashboards, SaaS, and commercial or monetisation plans in
            repository artefacts.
          - Claims that the tool predicts bugs, measures code quality, ranks
            developers, or replaces established code-health platforms.
          - Mandatory LLM use at runtime; agents may consume exports, but the
            product truth remains deterministic.
          - Making tree-sitter, LSP, or any language provider part of the
            initial critical path.
          - Feature-level execution plans, PRDs, packet plans, todos, or runs
            during this brief-capture step.
        durable_decisions:
          - "Adopt the public thesis: deterministic, local-first hotspot
            intelligence CLI for developers and coding agents."
          - "Adopt the tagline: Before you refactor, ask Git where the pain is."
          - Start conceptually with file-level Git-history evidence; defer
            symbol-level precision until provider seams are validated.
          - Treat tree-sitter as a later provider, alongside possible LSP,
            ctags, dependency, blame/history, and test/coverage providers.
          - Keep the public repository free of monetisation plans and SaaS
            strategy; use it as OSS/publicity framing only.
          - Use agent-ready reports as an export surface, not as model-driven
            product truth.
    prd_path: .flow/features/0030_ci-validation-workflow/prd.md
  requirements_contract:
    source_path: .flow/features/0030_ci-validation-workflow/prd.md
    source_digest: 654c21dcff52ea52fa13989f6432daece1a39391841561d2cf299b3228d01d6a
    extraction: structured_markdown
    requirements:
      - id: REQ-001
        source_ref: prd.md#requirements[1]
        text: Add one workflow under .github/workflows/.
        category: requirement
        packet_refs:
          - P1
        verification: null
        required_for_close_out: true
      - id: REQ-002
        source_ref: prd.md#requirements[2]
        text: Trigger the workflow on pull_request and push to the repository's primary
          branch.
        category: requirement
        packet_refs:
          - P1
        verification: null
        required_for_close_out: true
      - id: REQ-003
        source_ref: prd.md#requirements[3]
        text: Use a single Linux job, currently ubuntu-latest.
        category: requirement
        packet_refs:
          - P1
        verification: null
        required_for_close_out: true
      - id: REQ-004
        source_ref: prd.md#requirements[4]
        text: Use Zig 0.16.0, matching the documented supported version.
        category: requirement
        packet_refs:
          - P1
        verification: null
        required_for_close_out: true
      - id: REQ-005
        source_ref: prd.md#requirements[5]
        text: "Checkout must use full history (fetch-depth: 0) because validation and
          the product are Git-history-sensitive."
        category: requirement
        packet_refs:
          - P1
        verification: null
        required_for_close_out: true
      - id: REQ-006
        source_ref: prd.md#requirements[6]
        text: "Set least-privilege workflow permissions, at minimum contents: read."
        category: requirement
        packet_refs:
          - P1
        verification: null
        required_for_close_out: true
      - id: REQ-007
        source_ref: prd.md#requirements[7]
        text: "Run the canonical local validation gate:"
        category: requirement
        packet_refs:
          - P1
        verification: null
        required_for_close_out: true
      - id: REQ-008
        source_ref: prd.md#requirements[8]
        text: "``sh zig build validate ``"
        category: requirement
        packet_refs:
          - P1
        verification: null
        required_for_close_out: true
      - id: REQ-009
        source_ref: prd.md#requirements[9]
        text: "Run explicit Tree-sitter proof steps because they intentionally remain
          outside zig build validate:"
        category: requirement
        packet_refs:
          - P1
        verification: null
        required_for_close_out: true
      - id: REQ-010
        source_ref: prd.md#requirements[10]
        text: "``sh zig build tree-sitter-build-proof zig build tree-sitter-symbol-proof
          ``"
        category: requirement
        packet_refs:
          - P1
        verification: null
        required_for_close_out: true
      - id: REQ-011
        source_ref: prd.md#requirements[11]
        text: CI must not require or fake the sibling/local close-out smoke. That
          remains a local operator validation path.
        category: requirement
        packet_refs:
          - P1
        verification: null
        required_for_close_out: true
      - id: REQ-012
        source_ref: prd.md#requirements[12]
        text: CI logs must remain privacy-safe and must not upload raw reports or
          private evidence.
        category: requirement
        packet_refs:
          - P1
        verification: null
        required_for_close_out: true
      - id: REQ-013
        source_ref: prd.md#non-goals[1]
        text: No release automation, tags, changelog generation, package publishing,
          binaries, or artifact uploads.
        category: non_goal
        packet_refs:
          - P1
        verification: null
        required_for_close_out: false
      - id: REQ-014
        source_ref: prd.md#non-goals[2]
        text: No OS matrix, Zig version matrix, nightly schedule, cache, coverage
          service, CodeQL, dependency audit, badges, deployment, or
          package-manager integration.
        category: non_goal
        packet_refs:
          - P1
        verification: null
        required_for_close_out: false
      - id: REQ-015
        source_ref: prd.md#non-goals[3]
        text: No runtime product behaviour changes.
        category: non_goal
        packet_refs:
          - P1
        verification: null
        required_for_close_out: false
      - id: REQ-016
        source_ref: prd.md#non-goals[4]
        text: No workflow secrets.
        category: non_goal
        packet_refs:
          - P1
        verification: null
        required_for_close_out: false
      - id: REQ-017
        source_ref: prd.md#non-goals[5]
        text: No telemetry, upload, remote enrichment, or external analysis service.
        category: non_goal
        packet_refs:
          - P1
        verification: null
        required_for_close_out: false
      - id: REQ-018
        source_ref: prd.md#non-goals[6]
        text: No parser generation, submodules, or build-time network fetches beyond
          normal GitHub checkout and Zig setup.
        category: non_goal
        packet_refs:
          - P1
        verification: null
        required_for_close_out: false
      - id: REQ-019
        source_ref: prd.md#acceptance[1]
        text: A minimal workflow exists and is named clearly for validation.
        category: acceptance
        packet_refs:
          - P1
        verification: null
        required_for_close_out: true
      - id: REQ-020
        source_ref: prd.md#acceptance[2]
        text: Workflow triggers are limited to push and pull_request.
        category: acceptance
        packet_refs:
          - P1
        verification: null
        required_for_close_out: true
      - id: REQ-021
        source_ref: prd.md#acceptance[3]
        text: Workflow permissions are least-privilege and do not grant write access.
        category: acceptance
        packet_refs:
          - P1
        verification: null
        required_for_close_out: true
      - id: REQ-022
        source_ref: prd.md#acceptance[4]
        text: Workflow checks out full Git history.
        category: acceptance
        packet_refs:
          - P1
        verification: null
        required_for_close_out: true
      - id: REQ-023
        source_ref: prd.md#acceptance[5]
        text: Workflow installs or selects Zig 0.16.0 through a pinned or acceptable
          setup mechanism.
        category: acceptance
        packet_refs:
          - P1
        verification: null
        required_for_close_out: true
      - id: REQ-024
        source_ref: prd.md#acceptance[6]
        text: Workflow runs zig build validate.
        category: acceptance
        packet_refs:
          - P1
        verification: null
        required_for_close_out: true
      - id: REQ-025
        source_ref: prd.md#acceptance[7]
        text: Workflow runs zig build tree-sitter-build-proof.
        category: acceptance
        packet_refs:
          - P1
        verification: null
        required_for_close_out: true
      - id: REQ-026
        source_ref: prd.md#acceptance[8]
        text: Workflow runs zig build tree-sitter-symbol-proof.
        category: acceptance
        packet_refs:
          - P1
        verification: null
        required_for_close_out: true
      - id: REQ-027
        source_ref: prd.md#acceptance[9]
        text: Workflow does not include release, package, artifact-upload, cache,
          coverage, telemetry, secrets, or publish steps.
        category: acceptance
        packet_refs:
          - P1
        verification: null
        required_for_close_out: true
      - id: REQ-028
        source_ref: prd.md#acceptance[10]
        text: Local validation still passes after adding the workflow.
        category: acceptance
        packet_refs:
          - P1
        verification: null
        required_for_close_out: true
      - id: REQ-029
        source_ref: prd.md#acceptance[11]
        text: Public docs are changed only if needed; no README badge is required in
          this feature.
        category: acceptance
        packet_refs:
          - P1
        verification: null
        required_for_close_out: true
      - id: REQ-030
        source_ref: prd.md#edge-cases[1]
        text: If the chosen Zig setup mechanism is unpinned, unavailable, or requires
          broad permissions, stop and reshape or ask for operator approval.
        category: edge_case
        packet_refs:
          - P1
        verification: null
        required_for_close_out: true
      - id: REQ-031
        source_ref: prd.md#edge-cases[2]
        text: If zig build validate is too slow or flaky on hosted CI, stop and reshape;
          do not silently weaken CI to zig build test only.
        category: edge_case
        packet_refs:
          - P1
        verification: null
        required_for_close_out: true
      - id: REQ-032
        source_ref: prd.md#edge-cases[3]
        text: "If GitHub Actions shallow checkout changes validation behaviour, keep
          fetch-depth: 0 rather than changing the product or validation
          semantics."
        category: edge_case
        packet_refs:
          - P1
        verification: null
        required_for_close_out: true
      - id: REQ-033
        source_ref: prd.md#verification[1]
        text: Run git diff --check.
        category: verification
        packet_refs:
          - P1
        verification: Run git diff --check.
        required_for_close_out: true
      - id: REQ-034
        source_ref: prd.md#verification[2]
        text: Run zig build validate locally.
        category: verification
        packet_refs:
          - P1
        verification: Run zig build validate locally.
        required_for_close_out: true
      - id: REQ-035
        source_ref: prd.md#verification[3]
        text: Run zig build tree-sitter-build-proof locally.
        category: verification
        packet_refs:
          - P1
        verification: Run zig build tree-sitter-build-proof locally.
        required_for_close_out: true
      - id: REQ-036
        source_ref: prd.md#verification[4]
        text: Run zig build tree-sitter-symbol-proof locally.
        category: verification
        packet_refs:
          - P1
        verification: Run zig build tree-sitter-symbol-proof locally.
        required_for_close_out: true
      - id: REQ-037
        source_ref: prd.md#verification[5]
        text: Inspect the workflow for least-privilege permissions, full checkout
          history, Zig 0.16.0, and absence of prohibited
          release/upload/cache/secret steps.
        category: verification
        packet_refs:
          - P1
        verification: Inspect the workflow for least-privilege permissions, full
          checkout history, Zig 0.16.0, and absence of prohibited
          release/upload/cache/secret steps.
        required_for_close_out: true
      - id: REQ-038
        source_ref: prd.md#verification[6]
        text: Close-out does not require a real GitHub run, but the workflow syntax and
          local commands must be validated.
        category: verification
        packet_refs:
          - P1
        verification: Close-out does not require a real GitHub run, but the workflow
          syntax and local commands must be validated.
        required_for_close_out: true
    supporting_context:
      - source_ref: prd.md#problem[1]
        text: The project now has non-trivial Zig validation, vendored Tree-sitter
          sources, and inspect-only symbol evidence. Public contributors and
          publicity readers need a visible CI signal that the public validation
          gate passes on GitHub without adding release automation or changing
          runtime behaviour.
      - source_ref: prd.md#outcome[1]
        text: Add a minimal GitHub Actions validation workflow that runs the existing
          public validation contract on push and pull request. CI proves the
          public/default gate and explicit Tree-sitter proof steps only;
          close-out sibling/local repository smoke remains a local operator
          gate.
    notes:
      - Extracted requirements from PRD requirement, acceptance, behaviour,
        constraint, edge-case, verification, or case sections.
      - Supporting context and structural headings are not close-out required;
        kept 2 refs outside default requirement rows.
  acceptance_contract:
    outcome: Add a minimal GitHub Actions workflow that runs the public validation
      gate on push and pull request without release automation, uploads, cache,
      telemetry, or product behaviour changes.
    acceptance:
      - A single GitHub Actions workflow validates the project on push and pull
        request.
      - The workflow uses full checkout history because git-hotspots is
        Git-history-sensitive.
      - The workflow uses Zig 0.16.0.
      - The workflow runs `zig build validate`.
      - The workflow runs `zig build tree-sitter-build-proof` and `zig build
        tree-sitter-symbol-proof`.
      - The workflow has least-privilege read-only repository permissions.
      - The workflow contains no release, package, artifact-upload, cache,
        coverage, telemetry, secret, or publish steps.
      - Local validation remains green after adding the workflow.
  validation_contract:
    ladders:
      - V2_atomic_commit
      - V3_final_packet
      - V4_close_out
    review_sequence:
      - spec_compliance
      - code_quality
    packet_verification_commands:
      - git diff --check
      - zig build validate
      - zig build tree-sitter-build-proof
      - zig build tree-sitter-symbol-proof
    reviewer_must_repeat: true
    claim_classes: []
    evidence_policy:
      parity_claims: null
      synthetic_only_is_insufficient: false
    route_matrix:
      required: false
      routes: []
    resolution_matrix:
      required: false
      cases: []
    operator_examples:
      required: false
      cases: []
  ownership:
    reviewer_expectations:
      - verify acceptance against dispatch comparison basis
      - treat runner evidence as untrusted until independently checked
  decision_budget:
    allowed_local_decisions:
      - safe internal refactor inside dispatched scope
      - local implementation detail changes that do not alter contracts
    deferred_reconcile_triggers:
      - minor non-contract deviation worth reviewer visibility
      - implementation trade-off that stays inside acceptance
    hard_planner_escalation_triggers:
      - scope or acceptance change
      - dependency or prerequisite change
      - protected-surface mutation outside dispatch envelope
    protected_surfaces: []
  fallback_path: deferred_reconcile_decision
  notes: null
