schema: flow-feature.v1
feature:
  id: "0095"
  slug: provider-relationship-capability-matrix-refresh
  title: Provider relationship capability matrix refresh
  lane: standard
  status: ready
  autonomy_default: ask
intent:
  problem: After internal provider proofs, public capability claims can drift
    unless README, user guide, man page, help/explain text, fixtures, and
    validation are updated together from the closed outcomes.
  outcome: The public provider relationship capability matrix and validation
    surfaces accurately reflect only the lanes proven by 0091-0094, with
    deterministic fixtures and no overclaiming.
  acceptance:
    - Public support claims for Go, Lua, and Zig are updated only when their
      proof features closed with sufficient conformance and validation evidence.
    - README, user guide, developer guide, man page, help/explain text,
      fixtures, and tools/validate.sh remain aligned with inspectable provider
      behaviour.
    - Relationship table, JSON, and Markdown golden fixtures change only for
      admitted lanes and remain deterministic and privacy-safe.
    - Unsupported or no-admit lanes remain visibly unsupported or caveated
      without implying call-graph, dependency, type, ownership, scoring, or
      bug-prediction truth.
planning:
  state: ready
  summary: Refresh public relationship capability matrix and documentation after
    provider proofs close, admitting only proven lanes.
  brief_sync:
    status: updated
    reason: B002 linked and updated with the serial provider expansion batch; 0095
      owns the coordinated public capability matrix refresh after 0091-0094
      close.
    checked_at: 2026-05-29T00:46:10.847Z
  brief_alignment:
    status: aligned
    role: follow_up
    summary: 0095 follows 0091-0094 by updating public B002 provider relationship
      capability claims only from closed proof evidence and keeping unsupported
      lanes caveated.
    checked_at: 2026-05-29T00:46:15.575Z
proposal:
  lifecycle_state: shaping
  outcome_contract_ref: null
  risk_assessment:
    assurance_level: high_assurance
    reasons:
      - topology=serial_feature
      - assurance=high_assurance
      - review_mode=delegated_reviewer
      - runner_model=Strong
    risk_triggers:
      - standard_lane_delivery
      - feature_dependencies_present
      - public_api_or_platform_risk_keywords
    audit_required: true
    judge_review_recommended: true
    materialised_at: 2026-05-29T00:49:52.454Z
  quality_plan: null
project_feature:
  lifecycle_state: ready
  outcome_contract_ref:
    contract_kind: feature_intent
    contract_ref: feature.intent
    materialised_at: 2026-05-29T00:49:52.454Z
  quality_plan:
    assurance_band: high_assurance
    review_mode: delegated_reviewer
    reviewer_member_usage: required
    reviewer_expectations:
      - verify acceptance against dispatch comparison basis
      - treat runner evidence as untrusted until independently checked
    verification_contract:
      claim_classes: []
      evidence_policy:
        parity_claims: null
        synthetic_only_is_insufficient: false
      route_matrix:
        required: false
        routes: []
      resolution_matrix:
        required: false
        cases: []
      operator_examples:
        required: false
        cases: []
    derived_from_risk_assessment_ref: proposal.risk_assessment
    materialised_at: 2026-05-29T00:49:52.454Z
  authority_refinement:
    selected_sign_off_authority: orchestrator
    explicit_pause_required: true
    verification_independence_requirement: continuity_safe_delegated
    materialised_at: 2026-05-29T00:49:52.454Z
verification_independence_requirement: continuity_safe_delegated
readiness:
  requirements_ready: true
  acceptance_ready: true
  packet_plan_ready: true
  dependencies_ready: false
  implementation_ready: true
context:
  roadmap_item_id: null
  detached_reason: "Detached B002 successor batch: final public-surface refresh
    follows conformance and remaining-language proofs."
  depends_on_feature_ids:
    - "0094"
  blocked_by: []
design:
  rough_shape:
    - Read closed outcomes for 0091-0094 before changing public claims.
    - Update capability matrix, docs, help/explain text, man page, fixtures, and
      validation together for admitted lanes only.
    - Add drift checks that fail when public wording or fixtures disagree with
      provider behaviour.
  packets:
    - id: P1
      depends_on: []
      status: planned
      title: Outcome reconciliation
      goal: Read 0091-0094 outcomes and decide which lanes are publicly admitted.
      owner_phase: runner
      execution_context:
        - "Anchors: closed feature records for 0091-0094, B002, relation
          architecture docs."
        - "In scope: admission decision for Go, Lua, Zig based only on closed
          evidence."
        - "Out of scope: new provider implementation or reopening proofs by
          stealth."
        - "Protected: unsupported/no-admit lanes must stay unsupported in public
          surfaces."
        - "Dependencies: 0091-0094 closed."
        - "Validation/evidence: durable admission matrix with source feature ids
          and caveats."
        - "Review focus: public claims do not outrun evidence."
        - "Escalate if: a predecessor outcome is ambiguous or not closed."
        - "Done boundary: admitted and unsupported lanes are explicit before
          edits."
      verification_commands:
        - flow validate --target brief:B002
    - id: P2
      depends_on:
        - P1
      status: planned
      title: Public surface alignment
      goal: Update docs, help, man, fixtures, and validation for admitted lanes only.
      owner_phase: runner
      execution_context:
        - "Anchors: README.md, docs/user-guide.md, docs/developer-guide.md,
          man/git-hotspots.1, help/explain surfaces, fixtures,
          tests/integration.sh, tools/validate.sh."
        - "In scope: coordinated public wording, golden fixtures,
          inspect/help/explain matrix checks."
        - "Out of scope: scoring/ranking changes, non-admitted providers,
          browser UI."
        - "Protected: local-first, deterministic, evidence-only, no
          network/telemetry/cache-truth defaults."
        - "Dependencies: P1 admission decision."
        - "Validation/evidence: fixture diffs, prohibited claim scan, provider
          matrix drift checks."
        - "Review focus: every public surface agrees with actual provider
          behaviour."
        - "Escalate if: public output schema would require a new contract beyond
          additive provider support."
        - "Done boundary: docs/fixtures/validation are coherent and
          deterministic."
      verification_commands:
        - zig build test
        - zig build validate
    - id: P3
      depends_on:
        - P2
      status: planned
      title: Close-out smoke and privacy proof
      goal: Run final validation and close-out smoke for the public matrix refresh.
      owner_phase: runner
      execution_context:
        - "Anchors: tools/validate.sh close-out mode and sibling/local smoke
          commands."
        - "In scope: fresh validation evidence, privacy-safe smoke labels,
          bounded counts, caveat counts."
        - "Out of scope: raw reports, absolute private paths, remotes, authors,
          emails, commit messages."
        - "Protected: public report determinism and privacy assertions."
        - "Dependencies: P1 and P2 complete."
        - "Validation/evidence: git diff --check, zig fmt --check build.zig src
          tests, zig build test, zig build validate, close-out smoke or skip
          reason."
        - "Review focus: matrix refresh is complete enough for a fresh user and
          future planner."
        - "Escalate if: validation finds unrelated Flow or repo drift outside
          this feature."
        - "Done boundary: repo validates and public provider support claims are
          ready to close."
      verification_commands:
        - git diff --check
        - zig fmt --check build.zig src tests
        - zig build test
        - zig build validate
  output_contract:
    storage: Public docs, help/explain text, man page, fixtures, and validation updates.
    public_surface: Coordinated provider relationship capability claims for admitted
      lanes only.
    consumers:
      - CLI users
      - coding agents
      - runner
      - reviewer
    prohibited_content:
      - unsupported language overclaims
      - call graph truth
      - dependency proof
      - type checking claims
      - ownership
      - code quality judgement
      - developer metrics
      - bug prediction
      - network or telemetry claims
  packet_contract:
    version: v1
    owner_model: phase_owned
    review_sequence:
      - spec_compliance
      - code_quality
    verification_required: true
    required_packet_fields:
      - id
      - title
      - goal
      - depends_on
      - status
      - owner_phase
      - execution_context
      - verification_commands
execution:
  active_run_id: null
  current_packet_id: null
  next_packet_ids: []
  blockers: []
  requires_confirmation: true
links:
  epic_todo_id: null
  brief_ids:
    - B002
timestamps:
  created_at: 2026-05-29T00:39:28.992Z
  updated_at: 2026-05-29T00:51:05.990Z
dispatch:
  status: prepared
  prepared_at: 2026-05-29T00:51:05.990Z
  assurance_level: high_assurance
  control_model: split
  execution_topology: serial_feature
  model_plan:
    control_plane_model: Strong
    runner_model: Strong
    review_mode: delegated_reviewer
    review_model: Strong
    reviewer_member_usage: required
    research_model: null
    scaffold_owner_model: null
    fill_worker_model: null
    escalation_triggers:
      - repeated validation failures
      - dispatch envelope no longer fits implementation reality
      - public interface, persistence, security, or concurrency change
  execution_fit:
    fast_execute_eligible: false
    reason: Assurance level high_assurance requires stronger than Fast execution.
  scope:
    mode: packets
    feature_id: "0095"
    packet_ids:
      - P1
      - P2
      - P3
  comparison_basis:
    roadmap_item_id: null
    linked_brief_ids:
      - B002
    feature:
      title: Provider relationship capability matrix refresh
      problem: After internal provider proofs, public capability claims can drift
        unless README, user guide, man page, help/explain text, fixtures, and
        validation are updated together from the closed outcomes.
      outcome: The public provider relationship capability matrix and validation
        surfaces accurately reflect only the lanes proven by 0091-0094, with
        deterministic fixtures and no overclaiming.
      acceptance:
        - Public support claims for Go, Lua, and Zig are updated only when their
          proof features closed with sufficient conformance and validation
          evidence.
        - README, user guide, developer guide, man page, help/explain text,
          fixtures, and tools/validate.sh remain aligned with inspectable
          provider behaviour.
        - Relationship table, JSON, and Markdown golden fixtures change only for
          admitted lanes and remain deterministic and privacy-safe.
        - Unsupported or no-admit lanes remain visibly unsupported or caveated
          without implying call-graph, dependency, type, ownership, scoring, or
          bug-prediction truth.
    brief_alignment:
      status: aligned
      role: follow_up
      summary: 0095 follows 0091-0094 by updating public B002 provider relationship
        capability claims only from closed proof evidence and keeping
        unsupported lanes caveated.
      checked_at: 2026-05-29T00:46:15.575Z
    briefs:
      - id: B002
        title: Symbol-aware hotspot intelligence
        summary: "Capability brief for symbol-aware hotspot intelligence: extend
          git-hotspots beyond file-level evidence to identify historically
          active code symbols (functions, methods, types, modules, or other
          language-provider symbols) from local Git history using a
          language-agnostic pipeline. The capability must stay deterministic,
          local-first, and performant, while making new language support
          automatically benefit from the same history attribution, scoring,
          explanation, and future relationship analysis surfaces."
        desired_capability:
          - Report hotspot evidence at symbol granularity, not just file
            granularity, so users can investigate historically volatile
            functions, methods, types, modules, or provider-defined symbols.
          - "Use a normalized symbol-provider contract: language-specific
            providers only expose symbol identity, kind, file path, source
            range/span, and optional nesting/context; the Git-history
            attribution, scoring, output, and explanation pipeline remains
            language-agnostic."
          - Attribute local Git history to symbols by intersecting changed
            ranges, file revisions, and symbol spans in a deterministic way,
            with enough evidence for users to understand why a symbol appears
            hot.
          - Design the symbol model so future enrichers can ask which symbols
            are referenced by, used by, or related to hot places without
            redesigning hotspot evidence from scratch.
          - "Keep runtime defaults local-first: no network, telemetry, remote
            enrichment, or non-deterministic service dependency for symbol
            hotspot analysis."
        why:
          - File-level hotspots are useful but too coarse when a large file
            contains one volatile function or type and many stable neighbours.
          - A language-agnostic symbol pipeline prevents each future language
            provider from needing bespoke hotspot scoring and history-search
            logic.
          - Symbol-level evidence gives agents and humans a more precise
            investigation prompt while preserving the project rule that hotspots
            are evidence, not blame, predictions, or code-quality scores.
          - Future relationship analysis, such as symbols used by hot places,
            needs stable symbol identities and relation-ready output contracts
            from the beginning.
        in_scope:
          - Capability-level planning for extracting or receiving symbols
            through providers, normalizing symbol records, and mapping
            Git-history changes onto those records.
          - "Performance-sensitive history traversal strategy: batch Git
            queries, stream/limit revision data, parse only relevant file
            revisions where possible, and avoid naive full-repository reparsing
            for every commit."
          - "Deterministic evidence fields for symbol hotspots: churn/change
            frequency/recency/confidence/explanation inputs at symbol level,
            with links back to file path, symbol kind, range/source context, and
            relevant commits or changed hunks where feasible."
          - "Language-extensibility rule: adding a new symbol provider should
            make that language eligible for symbol hotspot reports without
            changing the scoring, history attribution, CLI report, or export
            logic except for provider registration and provider-specific tests."
          - "Forward-compatible seams for reference/use analysis: symbol
            identifiers, nesting/ownership, and relation-provider hooks should
            be planned even if reference graph support is deferred."
        out_of_scope:
          - Predicting bugs, judging code quality, ranking developers, or
            treating symbol hotspots as accusations rather than investigation
            prompts.
          - Network-backed code intelligence, hosted enrichment, telemetry, or
            remote index upload by default.
          - Implementing full semantic reference graphs, call graphs, or
            cross-language dependency intelligence in the first symbol-hotspot
            slice; those are future enrichers that should consume stable symbol
            evidence.
          - Requiring cache as product truth. Caches may improve performance
            later, but symbol analysis must have a correct no-cache path.
          - Browser-visible UI design is out of scope for this brief unless a
            future feature adds visual symbol hotspot exploration; if that
            happens, UX tone, visual constraints, and approval decisions must be
            captured in feature/PRD artefacts.
        durable_decisions:
          - Treat symbol hotspots as an extension of deterministic Git-history
            evidence, not as semantic judgement or prediction.
          - "Make the core symbol-hotspot pipeline provider-neutral: providers
            supply symbols; the shared engine maps history to symbols and
            produces common reports/exports."
          - Design for performance from the first feature by avoiding per-commit
            whole-tree parsing and by batching Git/history work.
          - Plan relation-aware output seams now, but defer reference/use
            analysis until after symbol hotspot evidence is trustworthy.
          - No feature-level PRD or implementation packet is created yet from
            this intake; the next shaping step should decide whether to create a
            feature for the first symbol-provider/history-attribution slice and
            push detailed acceptance and packet plans into feature.yaml/prd.md.
          - "First implementation slice selected during shaping for feature
            0078: project-level current-symbol hotspot evidence over retained
            ranked file hotspots using existing current symbol providers and
            optional HEAD current-line Git evidence. This deliberately is not
            true historical hunk-to-symbol attribution, symbol rename/move
            lineage, reference/use analysis, scoring replacement, cache, or
            provider plugin infrastructure; those remain later capability work
            after current-symbol evidence is trustworthy."
          - "Successor architecture spike selected for feature 0079: define true
            historical hunk-to-symbol attribution before runtime implementation.
            The spike is architecture-only and should produce
            docs/historical-symbol-attribution-architecture.md, comparing
            approaches and selecting a deterministic no-checkout local pipeline
            over historical blobs, changed hunks, and revision-local provider
            symbols. It deliberately does not add a user-facing historical
            symbol engine, scoring changes, semantic lineage, reference
            analysis, cache truth, or remote/provider dependencies."
          - "Successor batch selected after feature 0080: feature 0081 is the
            prerequisite internal pipeline integration that wires historical
            hunk attribution into bounded ranked-file analysis without public
            output drift; feature 0082 is the primary user-facing opt-in
            historical symbol hotspot report that should execute serially after
            0081 and update CLI/report/docs/tests/validation together.
            Reference/use analysis remains deferred until the public historical
            symbol report contract is stable."
          - "Successor architecture spike selected for feature 0083: define
            symbol relationship evidence before runtime implementation. The
            spike is documentation-only and should produce
            docs/symbol-relationship-architecture.md, comparing provider
            approaches and defining relation endpoints, kinds, confidence,
            caveats, deterministic aggregation, performance limits, and a narrow
            local-first implementation successor. It deliberately does not add
            relation runtime analysis, CLI/report output, scoring changes, cache
            truth, dependencies, or public claim expansion."
          - "Relation evidence successor batch selected: 0084 internal relation
            provider proof, then 0085 internal relation aggregation integration,
            then 0086 opt-in public symbol relationship report. The batch
            remains serial: 0085 depends on 0084, and 0086 depends on 0084 and
            0085. Public relation output must wait until internal provider and
            aggregation proof are closed. Relation evidence stays local-first,
            deterministic, caveated, evidence-only, and must not alter
            file/symbol scoring or imply call-graph truth, dependency proof,
            ownership, code-quality judgement, developer metrics, or bug
            prediction."
          - "Relation provider expansion batch selected after feature 0086: 0087
            records the provider expansion strategy and admission rubric, 0088
            adds TypeScript/TSX/JavaScript relationship provider proof, 0089
            adds Rust relationship provider proof, and 0090 hardens the provider
            relationship capability matrix and validation. The batch remains
            serial: 0088 depends on 0087, 0089 depends on 0088, and 0090 depends
            on 0089. Public relationship claims must remain opt-in,
            deterministic, local-first, caveated, evidence-only, and must not
            imply call-graph truth, type-aware dependency proof, ownership,
            code-quality judgement, developer metrics, bug prediction, scoring
            changes, cache truth, network access, or telemetry."
          - "Feature 0090 closed the public provider capability decision:
            current symbols, current-line history, and historical-symbol hunk
            attribution use the Zig, Go, Python, JavaScript, Lua, Rust,
            TypeScript, and TSX provider lanes, with file-level fallback for
            unsupported historical blobs; symbol-relationship evidence is public
            only for Python, JavaScript, Rust, TypeScript, and TSX. Zig, Go,
            Lua, and unsupported current files have no public relationship
            provider. Documentation, help/explain text, man page, integration
            checks, and validation anchors must stay aligned with that matrix
            until a later feature explicitly admits another relationship lane."
          - "Provider hardening and remaining-language relationship batch
            selected after feature 0090: 0091 creates a shared relation provider
            conformance harness; 0092 adds internal Go relationship provider
            proof; 0093 adds internal Lua relationship provider proof; 0094
            evaluates and, if safe, adds internal Zig relationship provider
            proof or records a no-admit outcome; 0095 is the only public-surface
            refresh and may admit Go, Lua, or Zig relationship support only from
            closed proof evidence. The batch is serial: 0092 depends on 0091,
            0093 depends on 0092, 0094 depends on 0093, and 0095 depends on
            0094. Public claims remain local-first, deterministic, caveated,
            evidence-only, and must not imply call-graph truth, dependency
            proof, type checking, ownership, code-quality judgement, developer
            metrics, bug prediction, scoring changes, cache truth, network
            access, or telemetry."
    prd_path: .flow/features/0095_provider-relationship-capability-matrix-refresh/prd.md
  requirements_contract:
    source_path: .flow/features/0095_provider-relationship-capability-matrix-refresh/prd.md
    source_digest: f3032b47e8fc4884c35cc35ca91fd31d78ce5d5a388f7f60803cb8031f8d2a95
    extraction: structured_markdown
    requirements:
      - id: REQ-001
        source_ref: prd.md#requirements[1]
        text: REQ-001 - Depend on closed features 0091, 0092, 0093, and 0094.
        category: requirement
        packet_refs: []
        verification: null
        required_for_close_out: true
      - id: REQ-002
        source_ref: prd.md#requirements[2]
        text: REQ-002 - Read predecessor outcomes before changing public support claims.
        category: requirement
        packet_refs: []
        verification: null
        required_for_close_out: true
      - id: REQ-003
        source_ref: prd.md#requirements[3]
        text: REQ-003 - Admit Go, Lua, or Zig relationship support only when the
          matching proof feature closed with sufficient conformance, fixture,
          validation, and privacy-safe evidence.
        category: requirement
        packet_refs: []
        verification: null
        required_for_close_out: true
      - id: REQ-004
        source_ref: prd.md#requirements[4]
        text: REQ-004 - Keep no-admit or unsupported lanes visibly unsupported or
          caveated in README, user guide, developer guide, manual page,
          help/explain text, fixtures, and validation.
        category: requirement
        packet_refs: []
        verification: null
        required_for_close_out: true
      - id: REQ-005
        source_ref: prd.md#requirements[5]
        text: REQ-005 - Update README, docs/user-guide.md, docs/developer-guide.md,
          man/git-hotspots.1, CLI help/explain output, integration fixtures, and
          tools/validate.sh together when public claims change.
        category: requirement
        packet_refs:
          - P2
        verification: null
        required_for_close_out: true
      - id: REQ-006
        source_ref: prd.md#requirements[6]
        text: REQ-006 - Keep relationship table, JSON, and Markdown golden fixtures
          deterministic and privacy-safe.
        category: requirement
        packet_refs: []
        verification: null
        required_for_close_out: true
      - id: REQ-007
        source_ref: prd.md#requirements[7]
        text: REQ-007 - Add or preserve drift checks proving public documentation and
          inspectable provider behaviour agree.
        category: requirement
        packet_refs: []
        verification: null
        required_for_close_out: true
      - id: REQ-008
        source_ref: prd.md#requirements[8]
        text: REQ-008 - Preserve existing file ranking, symbol ranking, historical
          symbol evidence, scoring, cache behaviour, network behaviour,
          telemetry defaults, and report schema compatibility.
        category: requirement
        packet_refs: []
        verification: null
        required_for_close_out: true
      - id: REQ-009
        source_ref: prd.md#requirements[9]
        text: REQ-009 - Keep caveats visible for syntax-only evidence, unresolved
          targets, unsupported languages, parser failures, dynamic dispatch,
          macro or generated code, package/module resolution gaps, type checking
          gaps, and cap hits.
        category: requirement
        packet_refs: []
        verification: null
        required_for_close_out: true
      - id: REQ-010
        source_ref: prd.md#requirements[10]
        text: REQ-010 - Run fresh close-out validation including zig build test, zig
          build validate, and privacy-safe real-repo smoke or explicit skip
          reason.
        category: requirement
        packet_refs:
          - P3
        verification: null
        required_for_close_out: true
      - id: REQ-011
        source_ref: prd.md#edge-cases[1]
        text: A predecessor closes with a no-admit result; public docs must not imply
          support.
        category: edge_case
        packet_refs: []
        verification: null
        required_for_close_out: true
      - id: REQ-012
        source_ref: prd.md#edge-cases[2]
        text: A language has current-symbol support but no admitted relationship
          provider.
        category: edge_case
        packet_refs: []
        verification: null
        required_for_close_out: true
      - id: REQ-013
        source_ref: prd.md#edge-cases[3]
        text: A provider emits internal evidence but public report support is deferred.
        category: edge_case
        packet_refs: []
        verification: null
        required_for_close_out: true
      - id: REQ-014
        source_ref: prd.md#edge-cases[4]
        text: Public table, JSON, and Markdown fixtures must sort deterministically.
        category: edge_case
        packet_refs: []
        verification: null
        required_for_close_out: true
      - id: REQ-015
        source_ref: prd.md#edge-cases[5]
        text: A validation drift check disagrees with docs or help output.
        category: edge_case
        packet_refs: []
        verification: null
        required_for_close_out: true
      - id: REQ-016
        source_ref: prd.md#edge-cases[6]
        text: Close-out smoke is unavailable and needs a privacy-safe skip reason.
        category: edge_case
        packet_refs:
          - P3
        verification: null
        required_for_close_out: true
      - id: REQ-017
        source_ref: prd.md#verification[1]
        text: git diff --check
        category: verification
        packet_refs: []
        verification: git diff --check
        required_for_close_out: true
      - id: REQ-018
        source_ref: prd.md#verification[2]
        text: zig fmt --check build.zig src tests
        category: verification
        packet_refs: []
        verification: zig fmt --check build.zig src tests
        required_for_close_out: true
      - id: REQ-019
        source_ref: prd.md#verification[3]
        text: zig build test
        category: verification
        packet_refs: []
        verification: zig build test
        required_for_close_out: true
      - id: REQ-020
        source_ref: prd.md#verification[4]
        text: zig build validate
        category: verification
        packet_refs: []
        verification: zig build validate
        required_for_close_out: true
      - id: REQ-021
        source_ref: prd.md#verification[5]
        text: Close-out smoke with a privacy-safe sibling/local label or explicit skip
          reason.
        category: verification
        packet_refs:
          - P3
        verification: Close-out smoke with a privacy-safe sibling/local label or
          explicit skip reason.
        required_for_close_out: true
    notes:
      - Extracted requirements from PRD requirement, acceptance, behaviour,
        constraint, edge-case, verification, or case sections.
  acceptance_contract:
    outcome: The public provider relationship capability matrix and validation
      surfaces accurately reflect only the lanes proven by 0091-0094, with
      deterministic fixtures and no overclaiming.
    acceptance:
      - Public support claims for Go, Lua, and Zig are updated only when their
        proof features closed with sufficient conformance and validation
        evidence.
      - README, user guide, developer guide, man page, help/explain text,
        fixtures, and tools/validate.sh remain aligned with inspectable provider
        behaviour.
      - Relationship table, JSON, and Markdown golden fixtures change only for
        admitted lanes and remain deterministic and privacy-safe.
      - Unsupported or no-admit lanes remain visibly unsupported or caveated
        without implying call-graph, dependency, type, ownership, scoring, or
        bug-prediction truth.
  validation_contract:
    ladders:
      - V2_atomic_commit
      - V3_final_packet
      - V4_close_out
    review_sequence:
      - spec_compliance
      - code_quality
    packet_verification_commands:
      - flow validate --target brief:B002
      - zig build test
      - zig build validate
      - git diff --check
      - zig fmt --check build.zig src tests
    reviewer_must_repeat: true
    claim_classes: []
    evidence_policy:
      parity_claims: null
      synthetic_only_is_insufficient: false
    route_matrix:
      required: false
      routes: []
    resolution_matrix:
      required: false
      cases: []
    operator_examples:
      required: false
      cases: []
  ownership:
    reviewer_expectations:
      - verify acceptance against dispatch comparison basis
      - treat runner evidence as untrusted until independently checked
      - confirm every public provider claim matches actual supported behaviour
        and closed proof evidence
  decision_budget:
    allowed_local_decisions:
      - safe internal refactor inside dispatched scope
      - local implementation detail changes that do not alter contracts
      - admit only provider lanes proven by closed predecessor evidence
    deferred_reconcile_triggers:
      - minor non-contract deviation worth reviewer visibility
      - implementation trade-off that stays inside acceptance
      - predecessor no-admit result reflected as unsupported public claim
    hard_planner_escalation_triggers:
      - scope or acceptance change
      - dependency or prerequisite change
      - protected-surface mutation outside dispatch envelope
      - public claim unsupported by closed evidence
      - report schema compatibility break
    protected_surfaces:
      - public CLI flags
      - public report schema
      - README/user guide/developer guide/man capability claims
      - help and explain wording
      - fixtures and validation matrix
      - file ranking and scoring
      - network telemetry or cache defaults
  fallback_path: deferred_reconcile_decision
  notes: "Serial dependency: execute only after 0094 closes. This is the only
    public-surface refresh; admit Go/Lua/Zig only from closed proof evidence."
